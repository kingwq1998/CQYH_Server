# 安全审计报告 / Security Audit Report

**审计日期**: 2026-05-23（含深度审计补充）
**审计范围**: YH_Server_Code 全代码库（游戏服务器 / 账号服务器 / 游戏登录器）
**审计方法**: 静态代码分析 + 模式扫描 + 架构走查 + 死代码分析 + 后门扫描
**实际代码量**: ~6,444 行 .cs 文件（不计 bin/obj 和辅助文件）

## 重要前提

代码库中存在两个使用 hex 风格命名空间的辅助文件夹（本次审计前的状态）：
- `游戏服务器/------------/` —— 未被任何模块引用，**0 引用**，本次已整体删除
- `游戏服务器/----/` —— 含真实业务代码（HTTP 充值/管理接口、AI 寻路），命名空间和类名为 hex 风格但字段名和逻辑清晰可读，本次已重命名为 `WebApi` / `AutoBattle` 等可读形式

深度审计还发现一处**隐蔽的外发逻辑**和一个**形同虚设的签名机制**，本次已修复。

---

## 一、严重程度分布

| 严重程度 | 数量 | 已自动修复 | 标记为协议级问题 |
|---------|------|----------|--------------|
| 🔴 CRITICAL | 9 | 5 | 4 |
| 🟠 HIGH | 8 | 2 | 6 |
| 🟡 MEDIUM | 5 | 0 | 5 |

"协议级问题"指修复会破坏现有客户端兼容性、需要客户端配合升级，无法在仅修改服务端代码的情况下完整解决。这些问题在报告中详细记录，但本次提交未自动应用，留给项目方做架构层决策。

---

## 二、已修复漏洞详情

### 🔴 [CRIT-01] Newtonsoft.Json TypeNameHandling RCE

**位置**:
- [游戏服务器/游戏服务器/模板类/序列化类.cs:24](游戏服务器/游戏服务器/模板类/序列化类.cs#L24)
- [账号服务器/序列化类.cs:18](账号服务器/序列化类.cs#L18)

**风险**: `TypeNameHandling = TypeNameHandling.Auto` 允许 JSON 中的 `$type` 字段指定任意 .NET 类型。结合 `JsonConvert.DeserializeObject` 加载来自磁盘文件（账号文件、游戏数据）的内容，攻击者只要能写入数据目录就能通过反序列化 gadget 链获得 RCE。已知 gadget 包括 `System.Windows.Data.ObjectDataProvider`、`System.Configuration.Install.AssemblyInstaller` 等。

**修复**: 引入 `ISerializationBinder` 实现（`安全类型绑定器`），白名单仅允许 `游戏服务器` / `账号服务器` / `Assembly-CSharp` 三个程序集的类型反序列化，其他全部抛 `SerializationException`。`TypeNameHandling.Auto` 保留以维持多态序列化（技能任务子类等），但攻击面被切断。

### 🔴 [CRIT-02] 门票生成使用 System.Random（可预测）

**位置**: [账号服务器/账号数据.cs:7,35](账号服务器/账号数据.cs#L7)

**风险**: `System.Random` 用系统时钟做种子，输出可预测。32 字符的会话门票（`ULS21-XXXXXXXXXX...`）名义上有 ~190 比特熵，但实际只有 ~32 比特种子熵。攻击者只要知道账号服务器启动时间区间，就能脱机暴力枚举 token，劫持任意账号。

**修复**: 替换为 `System.Security.Cryptography.RandomNumberGenerator.Create()`，从内核 CSPRNG 取真随机字节。

### 🔴 [CRIT-03] 账号文件写入路径穿越

**位置**: [账号服务器/主窗口.cs:113](账号服务器/主窗口.cs#L113)

**风险**: `File.WriteAllText(数据目录 + "\\" + 账号.账号名字 + ".txt", ...)`。`账号名字` 来自客户端注册请求，未经任何过滤。攻击者注册账号名 `..\..\..\Windows\System32\drivers\etc\hosts`（或类似）即可向任意路径写入 JSON 序列化数据，导致系统文件被破坏或远程代码执行（写入 Startup 目录、计划任务等）。

**修复**: 增加 `是合法账号名()` 白名单校验（仅允许字母、数字、`_`、`-`，长度 ≤ 64），并使用 `Path.GetFullPath` 做规范化 + 前缀校验，确保最终路径仍在 `数据目录` 之内。

### 🔴 [CRIT-04] 怪物爆率文件路径穿越

**位置**: [游戏服务器/游戏服务器/模板类/游戏怪物.cs:330](游戏服务器/游戏服务器/模板类/游戏怪物.cs#L330)

**风险**: 怪物配置文件中 `array[1]` 可包含 `../` 序列，导致 `File.OpenText` 越权读取任意文件。如果配置文件本身可被低权限角色或外部数据源写入，则攻击者可读取服务器密钥、日志等敏感文件。

**修复**: 增加字符白名单 + `Path.GetFullPath` 前缀校验。

### 🟠 [HIGH-01] TCP 监听 IP 封禁绕过

**位置**: [游戏服务器/游戏服务器/网络类/网络服务网关.cs:395-399](游戏服务器/游戏服务器/网络类/网络服务网关.cs#L395)

**风险**: 被封 IP 的连接虽然调用了 `Close()`，但**紧接着仍然被 `Enqueue` 加入待处理队列**。这是逻辑错误（缺少 `else`），导致 IP 封禁完全失效。

**修复**: 改为 `if/else` 结构。注意没有直接用 `return`，因为函数末尾还有 `BeginAcceptTcpClient` 重新挂载监听器，提前 return 会导致服务器停止接受新连接。

### 🟠 [HIGH-02] 封包字节字段无上界（DoS / 协议越界）

**位置**: [游戏服务器/游戏服务器/网络类/游戏封包.cs:67-68](游戏服务器/游戏服务器/网络类/游戏封包.cs#L67)

**风险**: 当 `描述符.长度 == 0` 时，长度从流中读取 `UInt16`（客户端控制，最大 65535）。`ReadBytes(n)` 会在堆上预先分配 `n` 字节。配合连接并发，可批量制造内存压力；同时还可能导致越界读取下一个封包的数据，造成协议解析错乱。

**修复**: 增加双重校验 —— ① 上限不超过 64KB；② 不超过流剩余可读字节数。任一越界直接抛异常。

### 🟠 [HIGH-03] 弱 TLS 协议版本

**位置**: [游戏服务器/游戏服务器/SMain.cs:93](游戏服务器/游戏服务器/SMain.cs#L93)

**风险**: 启用了 `Tls`（即 TLS 1.0，BEAST 攻击）和 `Tls11`（POODLE 攻击）。这两个版本已被 RFC 8996 正式废弃。

**修复**: 改为 `Tls12 | Tls13`。

---

## 三、未自动修复（协议级 / 架构级问题）

以下问题需要客户端配合或大规模架构改造，不在本次自动修复范围内。建议项目方按优先级排期处理。

### 🔴 [PROTO-01] 明文密码存储与比对

**位置**:
- [账号服务器/账号数据.cs:22](账号服务器/账号数据.cs#L22) — `public string 账号密码;`
- [账号服务器/网络通信.cs:89,174](账号服务器/网络通信.cs#L89) — `array[3] != value.账号密码` 直接字符串比对
- [游戏服务器/游戏服务器/地图类/玩家实例.cs:21394](游戏服务器/游戏服务器/地图类/玩家实例.cs#L21394) — 二级密码同样明文

**风险**: 账号密码以明文存储在 `.txt` 文件中，登录时直接字符串比较（同时有时序攻击风险）。账号文件泄露 = 全部用户凭据泄露。

**修复方向**:
1. 引入密码哈希：使用 PBKDF2（.NET Framework 自带 `Rfc2898DeriveBytes`）、bcrypt 或 Argon2id
2. 加盐：每个账号独立随机盐
3. 使用 `CryptographicOperations.FixedTimeEquals` 做常量时间比较
4. 迁移路径：登录时检测旧明文格式 → 升级为哈希 → 重写文件

### 🔴 [PROTO-02] 密码明文经 UDP 传输

**位置**: [游戏登录器/登录界面.cs:224](游戏登录器/登录界面.cs#L224)，[账号服务器/网络通信.cs:29](账号服务器/网络通信.cs#L29)

**风险**: 登录器通过 UDP 明文发送 `账号 密码`。攻击者抓包即得凭据，存在中间人攻击。

**修复方向**:
1. 短期：在客户端和账号服务器之间增加共享对称密钥的简单握手 + AES-GCM 加密
2. 长期：迁移到 TCP + TLS / DTLS

### 🔴 [PROTO-03] 账号服务器 → 游戏服务器之间无身份认证

**位置**: [游戏服务器/游戏服务器/网络类/Http门票接收器.cs:73](游戏服务器/游戏服务器/网络类/Http门票接收器.cs#L73)

**风险**: 原本有的 IP 白名单校验被注释掉。游戏服务器接收 UDP 6678 端口的门票数据**未做任何来源验证**。任何能访问 6678 端口的客户端可注入伪造门票 `<ticket>;<account>` → 直接以任意账号登录，无需密码。

**修复方向**:
1. 取消注释 IP 白名单校验，且 IP 必须从配置文件读取
2. 增加 HMAC-SHA256：账号服务器和游戏服务器配置共享密钥，门票格式改为 `<ticket>;<account>;<timestamp>;<hmac>`
3. 防止时间戳重放（5 分钟有效窗口 + nonce 去重）

### 🔴 [PROTO-04] 多个未鉴权的封包处理器

**位置**: [游戏服务器/游戏服务器/网络类/客户网络.cs:553-688](游戏服务器/游戏服务器/网络类/客户网络.cs#L553)

**风险**: 至少 8 个封包处理器（传奇之力激活、战功信息、战功任务、战功奖励、战功军令、战功积分、主题礼包、获取真实 IP）**没有检查认证状态**（"阶段" 字段）。未登录的客户端可触发这些处理器，部分还会对 `this.绑定角色` 解引用 → 空指针崩溃 / DoS。

**修复方向**:
1. 在每个 `处理封包` 入口统一加 `阶段` 检查（要求 ≥ 已登录）
2. 使用 `[需要登录]` 自定义 attribute + 反射统一拦截，避免每个 handler 单独写
3. 处理 `this.绑定角色?.` 做 null 判断

### 🟠 [PROTO-05] GM 权限基于角色数据中可篡改的标志位

**位置**: [游戏服务器/游戏服务器/地图类/玩家实例.cs:367](游戏服务器/游戏服务器/地图类/玩家实例.cs#L367)

**风险**: `管理员模式` 只读取 `角色数据.管理员角色.V`。如果角色数据文件可被服务器外部写入（参见 PROTO-03 的反序列化路径），攻击者可自我提权。

**修复方向**: 维护服务端独立的 GM 账号白名单（外部配置文件 + 校验和），不依赖角色数据。

### 🟠 [PROTO-06] License 模块自研 XOR 加密

**位置**: [游戏服务器/LicenseTool/LicenseInfo.cs:52](游戏服务器/LicenseTool/LicenseInfo.cs#L52)

**风险**: 单字节 XOR（key=5）+ 硬编码 RSA 公钥。License 本身的完整性保护极弱，可被任意篡改。

**修复方向**: 改用 AES-GCM + 服务器端持有的 RSA 私钥签名验证。

### 🟠 [PROTO-07] MD5 用于设备指纹和 HTTP 请求签名

**位置**:
- [游戏服务器/游戏服务器/地图类/玩家实例.cs:3943](游戏服务器/游戏服务器/地图类/玩家实例.cs#L3943)
- [游戏服务器/----/_0015_0003_0007_000E_000D_000D.cs:162,175](游戏服务器/----/_0015_0003_0007_000E_000D_000D.cs#L162)

**风险**: MD5 已知碰撞攻击实用。用于请求签名意味着攻击者可构造内容相同 hash 的恶意请求。

**修复方向**: 改为 HMAC-SHA256（不只是 SHA256，要带密钥 HMAC，否则同样可被附加扩展攻击）。

### 🟠 [PROTO-08] 客户端发来的 IP 被无验证地接受

**位置**: [游戏服务器/游戏服务器/网络类/客户网络.cs:553-556](游戏服务器/游戏服务器/网络类/客户网络.cs#L553)

**风险**: 处理器接收 `获取真实IP地址` 封包后直接覆盖 `this.网络地址`。攻击者可用任意 IP 污染日志、绕过基于 IP 的限流和封禁。

**修复方向**: 永远以 socket 实际 RemoteEndPoint 为准。如果是 CDN/反向代理场景，从 `X-Forwarded-For` 等可信源头读取，并配合受信前端列表。

### 🟡 [MISC-01] 账号注册无速率限制 / CAPTCHA 校验

**位置**: [账号服务器/网络通信.cs:100](账号服务器/网络通信.cs#L100)

**风险**: 没有 per-IP 限流，攻击者可批量注册账号枚举 / 占用名字。

**修复方向**: 引入滑动窗口限流（如每 IP 5 分钟内最多 3 次注册），服务端验证图形验证码。

### 🟡 [MISC-02] 密码无复杂度要求

**位置**: [账号服务器/网络通信.cs:107](账号服务器/网络通信.cs#L107)

**风险**: 只检查长度 6-18。"123456" 这种密码可通过。

**修复方向**: 服务端强制要求至少包含 2 种字符类型；可接入弱密码字典拦截（HaveIBeenPwned API 离线版本）。

### 🟡 [MISC-03] System.Random 用于游戏机制（非安全场景）

**位置**: [游戏服务器/游戏服务器/主程.cs:1105](游戏服务器/游戏服务器/主程.cs#L1105)

**风险**: 怪物掉落、概率等用 `System.Random`。理论上玩家可通过观察来推测种子，预测稀有物品掉落时机。

**修复方向**: 关键经济道具掉落改用 `RandomNumberGenerator`。一般场景保留 `System.Random` 即可。

### 🟡 [MISC-04] UDP 端口无 DoS 防护

**位置**: [游戏服务器/游戏服务器/网络类/网络服务网关.cs:269](游戏服务器/游戏服务器/网络类/网络服务网关.cs#L269)

**风险**: UDP 6678 端口的门票接收无连接级速率限制，单 IP 可灌入 MB 级数据被 `Split` 拆分。

**修复方向**: per-IP 令牌桶 + 单包大小硬上限（不超过 4KB）。

### 🟡 [MISC-05] 节点编辑器剪贴板反序列化

**位置**: [游戏服务器/游戏服务器/工具类/节点编辑器.cs:202](游戏服务器/游戏服务器/工具类/节点编辑器.cs#L202)

**风险**: 服务端 GUI 工具从剪贴板读取 JSON 反序列化。属于本地攻击面（需要先入侵管理员桌面），优先级低。

**状态**: 已通过 [CRIT-01] 的 SerializationBinder 间接缓解（不允许任意类型）。

---

## 四、未发现的攻击面

- **SQL 注入**: 项目使用自定义二进制格式（`Data.db` / `Temp.db`）持久化数据，未发现任何 `SqlCommand` / `ExecuteReader`。
- **XML / XXE**: 未发现 `XmlDocument` / `XmlReader` 处理外部数据的代码路径。
- **命令注入 / Process.Start**: 唯一一处 `Process.Start("notepad.exe", ".\\Server.txt")` 完全硬编码，不构成漏洞。
- **LDAP 注入**: 未使用 `DirectoryEntry` / `DirectorySearcher`。
- **硬编码凭据**: `.cs` 和 `.config` 文件中未发现密码、API Key、连接串等明文。

---

## 四点五、深度审计补充修复

### 🔴 [DEEP-01] 休眠后门：硬件指纹外发 + 远程控制信道

**位置**: [游戏服务器/游戏服务器/地图类/玩家实例.cs:4010](游戏服务器/游戏服务器/地图类/玩家实例.cs#L4010)（原 `最优恢复` 方法）

**风险**: 该方法用 byte 数组解码字符串的方式**故意规避静态分析**：
- 解码出 `Win32_Processor` 字符串 → WMI 查询 CPU 硬件信息
- 解码出 `base/Uniqueverify` 路径，POST 硬件指纹到 `主程.LogWebSite + base/Uniqueverify`
- 远端可控制 `玩家实例.恢复量`（游戏机制）+ 主界面状态栏文本
- 触发条件：在线玩家 ≥ 5 时静默激活；激活后随机延迟 1000-3000 秒应用响应

`主程.LogWebSite` 默认为 null（赋值代码 `//主程.LogWebSite = "http://58.87.104.125:8081/";` 已在源码中被注释），所以**当前不会触发**。但只要任何路径设置了这个变量，整套外发逻辑立即激活。这是一条**远程控制 / 数据采集信道**。

**修复**: `最优恢复` 方法体清空，仅设置 `下次恢复` 时间和 `恢复量 = -1`。原始实现保留为 `最优恢复_已禁用_原始实现` 供审计参考，永不被调用（首行 `if (true) return;`）。

### 🔴 [DEEP-02] HTTP 充值/管理接口签名形同虚设

**位置**: [游戏服务器/----/_0015_0003_0007_000E_000D_000D.cs:172](游戏服务器/----/_0015_0003_0007_000E_000D_000D.cs#L172)（`Sign` 方法）

**风险**: 原签名算法是 `MD5(query_string + "&")`。"&" 是**硬编码常量**，任何看到源码的人都能算出有效签名。该签名保护以下 HTTP 端点：
- `/useCmd` —— 执行 GM 命令（当前对应的 `主程.AddWebEvent` 调用已注释，但端点本体可被重新激活）
- `/paymentcallback` —— 充值回调（同上）
- `/modifyRole` —— 改角色数据（同上）
- `/getRoleList` —— **批量导出全部角色数据（账号、名字、等级、战力、金币、银币、元宝），未注释，可被立即利用**

攻击者拿到源码即可构造任意签名，调用 `/getRoleList` 一次性拖走全服角色清单。

**修复**:
1. `Settings.cs` 新增 `充值签名密钥` 配置项，默认空字符串
2. `Sign()` 改用 `HMAC-SHA256(query_string, 充值签名密钥)`
3. 密钥为空时 `Sign()` 返回固定哨兵 `__signing_key_not_configured__`，与任何客户端签名都不可能相等 → 所有签名请求直接被拒
4. 签名比对改用 `CryptographicOperations.FixedTimeEquals` 防时序攻击

**部署提示**: 公开发布时 `充值签名密钥` 必须为空（默认安全）。运营方部署时需手动配置一个高熵随机字符串，且对外 HTTP 客户端要同步用同样的密钥做 HMAC-SHA256 签名。

### 🟡 [DEEP-03] 删除死代码缩小攻击面

- 删除整个 `游戏服务器/------------/` 文件夹（10 个文件）—— 这是 VM 解释器基础设施，`.csproj` 已通过 `<Compile Remove="------------\**" />` 排除编译，留着只占空间和审计负担
- 删除 `游戏服务器/----/_000A_0007_0009_0009_0003_000A_0003_0007.cs` —— 孤立的 ResourceManager 类，无任何引用

### 📋 待运营方决策（未自动修复）

以下问题受架构约束，未在本次自动应用：

- **/getRoleList 等管理端点保留**：通过 HMAC-SHA256 + 密钥保护后理论安全，但建议进一步加 IP 白名单（只允许内网管理终端访问）
- **`pay.tengcanol.com` 硬编码**：在 `主程.cs:794` 的 `HttpPostYY` 中。当前整个方法在 542-831 行的 `/* */` 块注释内，编译不生效。但在源码中可见，若有人取消注释会调用该 URL。建议如果运营方不打算使用该支付通道，把 URL 改成空字符串作为默认
- **`主程.LogWebSite` 变量**：本身已无默认值，但变量声明仍存在。激进修复可以删除该字段并清理所有 `LogWebSite + xxx` 的调用点（DEEP-01 修复后这些调用都到不了关键路径，但保留可读性也有审计价值）

---

## 五、本次提交修复一览

| Commit | 文件 | 漏洞 ID |
|--------|------|--------|
| 单一提交 | `游戏服务器/游戏服务器/模板类/序列化类.cs` | CRIT-01 |
| 单一提交 | `账号服务器/序列化类.cs` | CRIT-01 |
| 单一提交 | `账号服务器/账号数据.cs` | CRIT-02 |
| 单一提交 | `账号服务器/主窗口.cs` | CRIT-03 |
| 单一提交 | `游戏服务器/游戏服务器/模板类/游戏怪物.cs` | CRIT-04 |
| 单一提交 | `游戏服务器/游戏服务器/网络类/网络服务网关.cs` | HIGH-01 |
| 单一提交 | `游戏服务器/游戏服务器/网络类/游戏封包.cs` | HIGH-02 |
| 单一提交 | `游戏服务器/游戏服务器/SMain.cs` | HIGH-03 |

---

## 六、后续建议

1. **集成静态分析工具**：项目应接入 Roslyn analyzer / SonarQube / Microsoft Security Code Analysis 做持续扫描
2. **CI/CD 增加 secret scan**：使用 trufflehog / gitleaks 防止凭据进 git
3. **将 PROTO-01 到 PROTO-04 排进 Sprint**：这些是当前架构最严重的洞，明文密码 + 无认证门票转发可被任意远程攻击者拿来登录任意账号
4. **审计 `游戏服务器/----/` 目录下的 hex 命名文件**：该目录的 `_XXXX_XXXX.cs` 文件较多，应人工 review 是否包含未清理的旧版本代码或可疑逻辑（本次审计已对其中关键的 WebApi / AutoBattle 部分做了重命名 + 加固）

---

*本报告由自动化代码审计生成。所有 CRITICAL 和 HIGH 级别问题均已人工复核。*

---

## 七、第二轮深度审计补充（2026-05-27）

在第一轮审计稳定运行 4 天后，针对剩余攻击面再做一轮专项扫描，重点是**未鉴权封包入口**、**带宽放大**、**日志注入**与 **HTTP body 资源耗尽**。所有问题在本次提交内自动修复，三个项目 `dotnet build` 仍为 0 errors。

### 🟠 [PROTO-04 续] 7 处封包处理器仍未做阶段守卫

虽然 PROTO-04 把战功 / 主题礼包等 8 个处理器加上了认证检查，但本轮发现还有 7 处遗漏。攻击者在 `游戏阶段.正在登录` 阶段（即 TCP 已连接但未完成账号登录）仍可触发以下逻辑：

| 处理器 | 文件:行 | 影响 |
|--------|--------|------|
| `鉴定无相钥石` | [客户网络.cs:533](游戏服务器/网络类/客户网络.cs#L533) | 未登录即可让服务端 `SendRaw` 3 次，浪费带宽与统计计数 |
| `内挂物品过滤` | [客户网络.cs:574](游戏服务器/网络类/客户网络.cs#L574) | 未登录即可触发 `new BitArray()` + 多次哈希查表，轻量 DoS |
| `领取七天奖励` / `领取七天大奖` | [客户网络.cs:961-969](游戏服务器/网络类/客户网络.cs#L961) | 未登录调用业务方法（绑定角色为 null 时 `?.` 短路，但不应进入业务路径） |
| `验证动态密码` | [客户网络.cs:3622](游戏服务器/网络类/客户网络.cs#L3622) | 二级密码校验属登录后流程 |
| `组队拍卖竞价` | [客户网络.cs:4023](游戏服务器/网络类/客户网络.cs#L4023) | 未登录就能向他人队伍发起拍卖 |
| `玩家放弃任务` | [客户网络.cs:4015](游戏服务器/网络类/客户网络.cs#L4015) | 未登录即可触发任务逻辑 |

**修复**：统一加上 `if (this.当前阶段 != 游戏阶段.正在游戏 || this.绑定角色 == null) return;` 的入口守卫。

### 🟠 [DEEP-04] 自定义扩展封包：未鉴权 + 日志注入 + 体积无上限

**位置**：[客户网络.cs:4145](游戏服务器/网络类/客户网络.cs#L4145) `处理封包(自定义扩展封包 P)`

**风险**：原实现允许**任何未登录 TCP 客户端**发送任意 UTF-8 字符串，服务端直接 `主程.添加系统日志("收到自定义扩展协议ID 1:" + Encoding.UTF8.GetString(bytes))` 写入系统日志。攻击面有三：

1. **未鉴权**：未登录即可让服务端写日志 → 配合大流量可撑爆磁盘
2. **日志注入**：内容含 `\r\n` / ANSI 转义 / 控制字符可伪造日志行、欺骗运维
3. **体积无上限**：受 HIGH-02 全局 64KB 限制，但本协议合理上限远低于此

**修复**：① 加 `阶段 + 绑定角色` 检查；② 字段长度上限收紧到 1024 字节；③ 净化所有 < 0x20 与 0x7F 的控制字符后再写入日志。

### 🟠 [DEEP-05] 聊天日志注入（两个 UI 路径）

**位置**：
- [主程.日志.cs:56](游戏服务器/主程类/主程.日志.cs#L56) — `主程.添加聊天日志` (SMain 路径)
- [启动窗口/主窗口.cs:578](游戏服务器/启动窗口/主窗口.cs#L578) — 旧版 `主窗口.添加聊天日志`

**风险**：玩家聊天消息（广播/私聊/好友）byte[] 直接 `Encoding.UTF8.GetString(...)` 后拼到 UI TextBox 与磁盘日志文件中。攻击者发送含 ANSI 转义 / `\r\n` / NUL 字符的消息可：

- 破坏运维 UI 显示（多行 / 控制字符 / 终端劫持）
- 污染聊天日志归档文件（`File.WriteAllText` 写盘后无法用普通方式过滤）
- 配合后续日志分析工具的解析漏洞做二次攻击

**修复**：两条路径都新增 `净化聊天文本(byte[])` 入口净化：丢弃所有 `< 0x20` 与 `0x7F` 控制字符；单条上限截到 512 字符。

### 🟠 [DEEP-06] 聊天封包带宽放大

**位置**：
- [客户网络.cs:发送聊天信息](游戏服务器/网络类/客户网络.cs) — 全服广播 / 传音
- [客户网络.cs:发送好友聊天](游戏服务器/网络类/客户网络.cs) — 好友消息
- [客户网络.cs:发送社交消息](游戏服务器/网络类/客户网络.cs) — 私聊 / 行会 / 队伍

**风险**：单条聊天的 `字节数据` 字段虽受 HIGH-02 全局 64KB 上限保护，但没有更细粒度的上限。攻击者发送 64KB 的"广播"，服务端 `网络服务网关.发送封包(...)` 会向**所有在线连接**复制并下发 → 1 包入 = O(N * 64KB) 出。在 1000 人在线时即可让单条聊天放大成 60 MB 出向流量。

**修复**：在 3 个处理器入口加入 `单条聊天最大字节 = 2048` 硬上限，超出直接断连。

### 🟡 [DEEP-07] WebApi 三处加固

**位置**：[WebApi.cs:OnPostRequest](游戏服务器/网络类/WebApi.cs)

虽然 WebApiService 当前在 `网络服务网关.启动服务()` 里**整段被注释**（默认不监听），但代码本身存在隐患，一旦运营方取消注释即生效：

1. **HTTP body 无上限**：`new StreamReader(request.InputStream).ReadToEnd()` 直接读完客户端 body。攻击者用 Content-Length 极大的 POST 可撑爆内存。
2. **异常信息泄露**：`catch (Exception ex) { WriteResponse(response, "request error: " + ex); }` 把完整异常堆栈回写给攻击者，暴露 .NET 类型名、文件路径、内部方法签名。
3. **null 反序列化崩溃**：`JsonConvert.DeserializeObject<Dictionary<string, string>>(...)` 在 body 是无效 JSON 时返回 `null`，后续 `dictionary.TryGetValue` NPE → catch 后再叠加问题 2 的信息泄露。

**修复**：① 入口先校验 `Content-Length` ≤ 64KB；② 用 `ReadBlock` + `Peek` 双重确认未超长；③ 异常仅写服务端日志，对外返回固定 `"request error"`；④ `dictionary == null` 时返回 `"invalid json"`。

### 修复一览

| 编号 | 严重 | 文件 | 改动概述 |
|------|------|------|---------|
| PROTO-04 续 | 🟠 | `客户网络.cs` ×7 处 | 鉴权检查 |
| DEEP-04 | 🟠 | `客户网络.cs` 自定义扩展封包 | 鉴权 + 长度上限 + 日志净化 |
| DEEP-05 | 🟠 | `主程.日志.cs` / `启动窗口/主窗口.cs` | 聊天文本净化 |
| DEEP-06 | 🟠 | `客户网络.cs` ×3 处 | 聊天单条 2 KB 上限 |
| DEEP-07 | 🟡 | `WebApi.cs` | HTTP body 上限 + 异常脱敏 + null 防御 |

---

## 八、第三轮针对性审计 (2026-05-27)

针对传奇引擎常见漏洞模式 (GM 提权 / 元宝复制 / 二级密码 / 摆摊放大) 做专项扫描。

### 🟠 [HIGH-04] 商人发货命令整数溢出, 可让在线玩家元宝爆减

**位置**: [游戏服务器/管理命令/发货.cs:31-38](游戏服务器/管理命令/发货.cs#L31)

**风险**: `发货` (商人命令) 收到 `元宝数量 (uint)` 后做 `元宝数量 * 100`, 但未限制 `元宝数量` 上界。攻击者商人输入 `0x04000000` (≈67M), `*100 = 6.7e9` 会让 uint 回绕到 ≈2.4e9。后续两个问题串起来:

1. `商人.元宝数量 < num` 校验通过 (num 已是回绕值)
2. 接收方累加 `元宝数量.V + num > int.MaxValue` 也是 uint 加法, 大概率再回绕一次, 受害者元宝大幅减少甚至清零

虽然商人也得损失一部分元宝, 但作为对头操作 (商人是攻击者控制的合作账号), 可以用任意可控数值对受害者元宝执行"归零攻击"。同类漏洞已在 [`赠送元宝.cs:29`](游戏服务器/管理命令/赠送元宝.cs#L29) 修过 (`> uint.MaxValue / 100u` 上限), 本次同步到 `发货.cs`。

**修复**:
1. `发货.cs:31`: `元宝数量` 上限收紧为 `uint.MaxValue / 100u` (≈42M, 对齐 `赠送元宝`)
2. `发货.cs:55` 与 `赠送元宝.cs:52`: 累加溢出校验改用 `(ulong)元宝数量.V + num > int.MaxValue`, 防 uint+uint 回绕

### 已知风险但不在本次范围 (post-compromise)

下列属于"需要先攻陷服务器或账号"才能利用的二线问题, 按既定原则不在本次自动修复:

- **扣除元宝 / 扣除金币 uint 下溢**: [`扣除元宝.cs:21`](游戏服务器/管理命令/扣除元宝.cs#L21) 用 `Math.Max(0u, 元宝数量 - this.元宝数量)`, 但 `uint - uint` 在 `Math.Max` 前已回绕成大正值, 反而让 max 取到回绕值 → 误操作时受害者元宝爆增到 uint 最大。需要 GM 权限 (PROTO-05)。
- **添加元宝 uint*100 溢出校验自身溢出**: [`添加元宝.cs:20`](游戏服务器/管理命令/添加元宝.cs#L20) 校验式 `this.元宝数量 * 100` 本身就可能回绕, 让校验失效。同样需要 GM。
- **二级密码空账户自动设置**: [`玩家实例.cs:17274`](游戏服务器/地图类/玩家实例.cs#L17274) 未设置二级密码时, 第一次输入即被当作设置。攻击者拿到一级账号后第一时间登入即可霸占二级密码。属于一级密码失陷后的次生攻击, 不在本范围。
- **下发礼包码 继承 玩家命令 而非 GM 命令**: [`下发礼包码.cs:12`](游戏服务器/管理命令/下发礼包码.cs#L12) 继承关系不一致, 但 `执行命令(玩家)` 是空实现, 玩家路径不会触发真实逻辑, 实际无可利用面 (HTTP 触发路径已被注释). 标记为代码风格债, 不算可利用漏洞.

### 第三轮修复一览

| 编号 | 严重 | 文件 | 改动概述 |
|------|------|------|---------|
| HIGH-04 | 🟠 | `管理命令/发货.cs` | 元宝数量上限 + ulong 累加溢出校验 |
| HIGH-04 续 | 🟠 | `管理命令/赠送元宝.cs` | ulong 累加溢出校验, 对齐发货 |
| MISC-06 | 🟡 | `地图类/玩家实例.cs::玩家合成物品` | 死代码 `_ = this.交易状态;` 替换为真实的死亡/摆摊/交易状态守卫 |

---

## 九、第四轮针对性审计 (2026-05-27, 团队/UI 文本)

### 🟠 [HIGH-05] 队长强制踢人未清理被踢者拍卖, 金币锁死

**位置**: [游戏服务器/地图类/玩家实例.公会师门.cs:511](游戏服务器/地图类/玩家实例.公会师门.cs#L511)

**风险**: `申请队员离队` 中, 玩家自主离队 (line 461) 会调用 `所属队伍.放弃所有拍卖(角色数据)` 通过邮件退回竞价金币; 但**队长强制踢人**路径 (line 511) 没有这一步。

攻击场景: 攻击者作为队长可以在被踢者正在竞价拍卖时强制踢出对方, 让 `拍卖参与列表` 里被踢者的 `value.出价 > 0` 的金币留在锁定状态, 永远不会通过 "组队竞拍退钱" 邮件退还。

**修复**: 在队长踢人前补一行 `this.所属队伍.放弃所有拍卖(角色数据);`, 与自主离队路径保持一致。

### 🟡 [LOW-A] 行会公告 / 行会宣言 / 摊位名字 未净化控制字符与双向覆写

**位置**:
- [游戏服务器/地图类/玩家实例.公会师门.cs:1048](游戏服务器/地图类/玩家实例.公会师门.cs#L1048) — `更改行会公告`
- [游戏服务器/地图类/玩家实例.公会师门.cs:1081](游戏服务器/地图类/玩家实例.公会师门.cs#L1081) — `更改行会宣言`
- [游戏服务器/地图类/玩家实例.交易摆摊.cs:651](游戏服务器/地图类/玩家实例.交易摆摊.cs#L651) — `更改摊位名字`

**风险**: 行会公告/宣言/摊位名字 直接 `Encoding.UTF8.GetString(数据).Split('\0')[0]` 后存储并广播给行会成员或摆摊区周围玩家, 没有任何字符内容净化。攻击者可塞入:

- C0 控制字符 (`\r \n \b ESC 0x00-0x1F`): 破坏 UI 显示
- DEL (`0x7F`): 同上
- Unicode 双向覆写 (`U+202A..202E`, `U+2066..2069`): 让显示文本与实际存储反向, 用于钓鱼伪装 "系统通知" / 管理员公告

**修复**: 新增 partial-class 内 `净化展示文本(string raw)` 助手, 入口统一过滤上述三类危险字符; 摊位名字共用同一方法。

### 第四轮修复一览

| 编号 | 严重 | 文件 | 改动概述 |
|------|------|------|---------|
| HIGH-05 | 🟠 | `地图类/玩家实例.公会师门.cs::申请队员离队` | 队长踢人前补 `放弃所有拍卖`, 避免被踢者金币锁死 |
| LOW-A | 🟡 | 同上 + `玩家实例.交易摆摊.cs` | `净化展示文本` 助手过滤控制字符与 Unicode 双向覆写, 用于行会公告/宣言/摊位名字 |

---

## 十、第五轮 多代理工作流审计 (2026-05-30)

**方法**: 用多代理工作流对**远程攻击面**做专项审查 —— 18 个 finder 多模态并行扫描(账号服务器认证/封包解析核心/网关门票登录绑定/HTTP·WebApi/客户网络 302 个处理器分段/交易摆摊/行会师门组队拍卖/邮件仓库物品/经济整数运算/强化合成/挖矿挂机 + 跨文件专项: 整数溢出·路径穿越·反序列化·DoS放大·Lua 脚本层) → 53 条候选 → 每条候选经"可达性视角"与"利用机制视角"双重对抗验证 → 综合裁决去重排序得 23 条确认 + 3 条争议。再对全部 26 条逐条深读代码复核并产出精确修复规格，判定 **25 条 `real-fix-now` + 1 条误报**。

**范围(硬性)**: 仅修**远程可达**漏洞(攻击者能力 = 能开 TCP/UDP 连接收发任意封包，至多持一个自己注册的普通低权限账号)。**排除** post-compromise(需先拿 GM/管理员、已知受害者密码、能写服务器数据文件、本地桌面访问)的问题，按 [security-triage] 既定原则跳过。

全部 25 条已修复，三个项目 `dotnet build` 为 **0 errors / 0 warnings**。

### 🔴 [C05] 门票来源白名单 fail-open + 无签名门票 → 零凭证接管任意账号(含 GM)

**位置**: [网络服务网关.cs:门票来源放行](游戏服务器/网络类/网络服务网关.cs)、[Settings.cs:门票来源白名单/专用网关登录器 fallback](游戏服务器/Settings.cs)、[客户网络.cs:处理封包(客户账号登录)](游戏服务器/网络类/客户网络.cs)

**风险**: 门票 UDP 接收端绑定 `IPAddress.Any`；`门票来源放行` 在白名单集合为空时 `return true`(fail-open)，而 `Settings.门票来源白名单` 默认 `""`，故默认部署不过滤。门票为明文 `<ticket>;<account>` 无 HMAC/签名/时间戳，`客户账号登录` 收到后只校验门票存在/未过期/账号未在线，账号不存在自动建号、全程无口令校验。专用网关分支还支持 `按角色登录-<公开角色名>` 反查账号。攻击者零凭证(仅需能发 UDP+TCP)即可向门票端口注入任意账号门票后接管，目标若是 GM 账号则获得管理员。系 PROTO-03 的延续 —— 既有缓解 fail-open 默认不生效。

**修复**: `门票来源放行` 改 **fail-closed**：白名单未配置时仅放行环回地址(`IPAddress.IsLoopback`，覆盖同机账号服务器)，远程来源一律拒绝并按节流打日志；修正 `Settings` 读取 `专用网关登录器` 时误用 `下线宝宝不死` 作默认值的 bug。(跨机部署强烈建议进一步配置 HMAC 共享密钥，列为后续。)

### 经济 / 物品复制 / 越权 (🟠 High)

| ID | 位置 | 风险与修复 |
|----|------|-----------|
| C01 | `玩家实例.交易摆摊.cs::购买摊位物品` | 摆摊买断分支把满堆叠母对象整体交付买家且全程不递减 `当前持久`、又销毁卖家余量 → 挂单数量 < 真实堆叠时整堆被复制窃取。改为摊位记账与真实堆叠各按 `购买数量` 递减、买家恒收一个 `购买数量` 的新建子对象 |
| C02 | `客户网络.cs::处理封包(更改存储权限)` | 仅阶段守卫、无职位鉴权 → 任意会员自授行会仓库取出权限盗空公会仓库 / 清空会长权限。补会长校验 + 标志位 `& 0x0FFF` 裁剪 |
| C03 | `玩家实例.cs::玩家合成装备` | 合成参数槽位未去重 → 同一物品填多个配方位"一份料产整件"。改为按真实背包槽位累计需求做一次性校验 + 单次扣减 |
| C04 | `队伍数据.cs::竞价拍卖` | 不校验 `拍卖序号` 属当前活动件，用全局价格/角色比价改写 → 越序竞价劫持他人中标物品并吞出价。补"序号必须等于当前活动件序号"守卫 |
| C08 | `玩家实例.cs::装备精炼替换` | 临时精炼缓冲未绑定源装备 → 用廉价垫件刷结果后跨装备/跨分类套属性绕开槽料。绑定源装备(`ReferenceEquals`)+ 要求目标 `开启精炼>0` |
| C09 | `玩家实例.交易摆摊.cs::放入摊位物品` | 只拦 `是否绑定` 不拦 `灵魂绑定` → +9 灵魂绑定装备经摆摊跨账号套现。补灵魂绑定拦截，与交易/寄售/邮件/师门一致 |

### 认证暴破 / DoS (🟠 High)

| ID | 位置 | 风险与修复 |
|----|------|-----------|
| C06 | `账号服务器/网络通信.cs::取桶` 等 | per-IP 限速表满 5 万后 `取桶` 返回临时桶 → 封禁永不触发，伪造源 IP 撑表即可全局关闭暴破防护。表满对认证类请求改 **fail-closed**(恒封禁哨兵桶)+ 新增**按账号维度**失败封禁(20 次/5min→封 5min)与 per-IP 表解耦 |
| C07 | `游戏封包.cs::取封包` | 变长封包(`UnknownC644`/`敏感字符过滤`)总长字段填 0 → 游标零推进，接收回调 `while(true)` 无限重解析同一 4 字节(预鉴权、裸 TCP) → IO 线程钉死 + 接收列表 OOM。补"总长 < 4 抛异常断连"守卫 |
| D01 | `队伍数据.cs::放弃所有拍卖` | foreach 枚举活字典时 `拍卖分配物品` 内 `Remove` 同字典 → 集合已修改异常；`MaxBy` 空序列 → NRE。普通账号自建拍卖队即可远程触发，主循环线程路径会跳过整 tick。改快照键迭代 + `TryGetValue` 重取 + `MaxBy` 空守卫(同步修 `放弃拍卖` 同型 NRE) |

### 资源耗尽 / 配置 (🟡 Medium)

| ID | 位置 | 风险与修复 |
|----|------|-----------|
| C10 | `账号服务器/网络通信.cs` + `主窗口.cs` | 注册无账号总数上限 → 伪造 IP 灌文件耗尽磁盘/inode。加账号总数硬上限(默认 100 万) |
| C11 | `网络服务网关.cs` | `门票数据表` 无过期清理 → 只喷 UDP 不发 TCP 致无界增长 OOM。加周期清扫过期项 + 2 万条上限止血 |
| C12 | `Settings.cs:限制重要封包间隔时间` | 放大类处理器节流默认关闭。默认改为开启(运营仍可关) |
| D02 | `玩家实例.挖矿.cs::玩家开始挖矿` | 不校验武器槽 → 空武器栏使 `武器损失持久` 取不到武器，零成本无损耗刷矿(且产物近乎必出黑铁矿)。入口校验 0 号位武器存在且耐久 > 0 |

### 越权探测 / 逻辑 / 信息 (⚪ Low)

| ID | 位置 | 风险与修复 |
|----|------|-----------|
| C13 | `玩家实例.cs::玩家装备雕色` | `孔洞颜色`/`孔洞位置` 无边界校验直接索引固定 9 元素数组 → 越界异常自连接 DoS。补 `0..8` / `0≤x<Count` 校验(并修原 off-by-one + 负数放行) |
| C14 | `玩家实例.交易摆摊.cs::玩家打开摊位/购买摊位物品` | 无同地图/距离校验 → 跨地图远程扫描/买空任意在线摊位。补同地图 + 网格距离 ≤12 校验 |
| C15 | `玩家实例.cs::查看对象装备` | 单封包放大为全装备+天赋 4 个序列化下发包、无节流。加 300ms 无条件冷却 |
| C16 | `玩家实例.cs::查询我的寄售/查询寄售物品/查询指定物品` | 每次请求全表扫描寄售数据表、无冷却。加 500ms 无条件冷却 |
| C17 | `客户网络.cs::处理封包(特权引导寻路)` | 缺阶段守卫 → 登录前阶段 0 探测守卫 NPC 坐标。补标准阶段守卫 |
| C18 | `玩家实例.挖矿.cs::秒触发内容结果` | 发矿不复核当前地图 → 离开矿区(传送/换图)后续发矿。每 tick 复核地图，离图即停挖 |
| C19 | `客户网络.cs::处理封包(客户账号登录)` | 以客户端自报 MAC(可伪造)作网卡封禁键 → 伪造即绕过/嫁祸。移除该登录闸口，改用权威 socket IP(对齐 PROTO-08；GM 命令与存档仍保留 `网卡封禁` 字典) |
| C20 | `玩家实例.cs::判断主题礼包日期` | 周末分支条件 `<1 && >5` 恒为假 → 周末任意日期序号放行，跳过物品A白名单越权获取任意装备。`&&` 改 `||` + 查表失败即拒纵深 |
| C22 | `玩家实例.公会师门.cs::处理入会申请` | 审批同意未复检 100 人上限 → 成员突破上限致计数 `(byte)` 回绕 + 描述广播 O(N²) 放大。补 `Count>=100` 复检 |
| C23 | `Database/System/lua/Npc/NpcFace/6361.lua` | 付费传送 `else` 分支只判金币不扣费 → 副本内无限免费传送(金币 sink 失效)。补回扣费 |
| D03 | `Database/System/lua/Npc/NpcFace/6112.lua` | 妖塔组队金币前置校验误用队长(非各队员) → 金币不足 10 万的小号被免费传送进副本。校验对象改回当前队员 |

### 误报(已读码确认，不修)

- **C21** `游戏封包.string` 字段 `Split` 取首元素空内容越界：经逐行核实，可达的定长 string 字段路径不会触发空数组取 `[0]`，判误报。

### 仍建议后续处理(本轮范围外)

- C05 跨机部署补门票 **HMAC + 时间戳防重放**(需账号服务器侧同步)
- 覆盖缺口：邮件附件领取、月卡/购买并发、镶嵌/宝石合成对称性、好友/社交系统、`Database` 脚本数值校验位 等可作下一轮深挖。

---

## 十一、第六轮 多代理工作流审计（2026-06-02）

**方法**：4 个多代理工作流串联 —— ①18-finder 漏洞补扫 → 对抗式验证；②12 组修复完整性/绕过专项（核对前 5 轮约 40 个修复是否成立/可绕过/有同型未覆盖路径）；③3 个 critical 三视角独立复核 + **守卫矩阵系统枚举 131 个改背包/货币处理器** + 完整性批判；④收尾专项扫 6 轮未审子系统（进行中）。合计约 **140 个 agent / ~7.7M token**，每条候选经「可达性 + 利用机制」对抗验证后去重裁决。
**范围**：远程可达（攻击者收发任意封包 + 至多一个普通低权限账号），排除 post-compromise。
**状态**：本节为**发现清单（待修复）**；修复将在 `/plan` 后单独提交，届时补 `CHANGELOG` 安全修复条目。

### ⭐ 系统性发现：「改背包/货币处理器缺死亡/交易/摆摊守卫」dupe 家族

前 5 轮逐个修了 C01/C08/C09 等单点 dupe，但漏掉根因：一大批消耗/改动背包材料的处理器**入口缺少**与同库其它处理器一致的那句标准守卫
`if (this.对象死亡 || this.摆摊状态 > 0 || this.交易状态 >= 3) return;`。
攻击者在**面对面交易中（交易状态≥3）或摆摊挂单中（摆摊状态>0）**触发这些处理器：物品/材料虽被消耗，却仍被交易/摊位字典按引用持有，`交换物品`/`购买摊位物品` 仍将其交付对方/买家（且 `购买摊位物品` 第 828 行恒为买家新建对象）→ **数量守恒被破坏 = 复制**。守卫矩阵系统枚举确认 **9 组（15+ 方法）** 同此根因（见下表带 †）。根因级修复另见 11.8。

### 🔴 已确认 CRITICAL（4）

| ID | 漏洞 | 位置 | 复核 |
|----|------|------|------|
| R6-01 † | `玩家兑换精粹` 缺守卫 → 交易/摆摊中兑换 → 祖玛/赤月武器 + 武器精粹复制 | `地图类/玩家实例.cs:15770` | 守卫矩阵✓ |
| R6-02 † | `玩家铭文刻印` 缺守卫 → 铭文材料 90226 **×1000 级复制**；另：仅缺一种材料时对 null 列表调 `消耗背包物品` → 远程 NPE 自掉线 | `地图类/玩家实例.cs:11109` | 守卫矩阵✓ |
| R6-03 † | `行会仓库转入`（及 `转出`）缺守卫 → 复制交易/摆摊中的物品（并可能误删槽 0 物品） | `地图类/玩家实例.公会师门.cs:1557 / 1644` | 三视角 3/3 |
| R6-04 | **C01 修复回归**：`购买摊位物品` 对**非堆叠装备**把 `当前持久`(耐久)当数量递减，`<=0` 才删卖家母对象 → 卖家保留原装备 + 收 95% 金币、买家得副本 → 装备无限复制 | `地图类/玩家实例.交易摆摊.cs:809-828` | 三视角 3/3 |

### 🟠 已确认 HIGH（12）

| ID | 漏洞 | 位置 |
|----|------|------|
| R6-05 † | `玩家合成装备/合成勋章`、`玩家合成灵石`、`玩家合无相石` 缺守卫 → 材料 dupe | `地图类/玩家实例.cs:19287 / 15745 / 19052` |
| R6-06 † | `装备开启/重新/转移精炼` 三处缺守卫 → 精炼材料(开槽料/41001/41002) dupe | `地图类/玩家实例.cs:16246 / 16295 / 16430` |
| R6-07 † | `玩家装备雕色` 缺守卫 → 染色石 91116 dupe | `地图类/玩家实例.cs:15493` |
| R6-08 † | `玩家武器祈祷`（及兄弟 `角色防具升级`）缺守卫 → 祈祷材料 80003 dupe | `地图类/玩家实例.cs:19077 / 18563` |
| R6-09 † | `玩家武器铸魂` 缺守卫 → 铸魂材料 dupe | `地图类/玩家实例.cs:19239` |
| R6-10 | `申请发送邮件` 标题/正文 UTF8 字节长度未截断 → 收件人 `邮件检索/内容描述` 写固定 `byte[61]/[554]` 缓冲区 `CopyTo` 抛 `ArgumentException` → 受害者每次开邮箱即崩溃掉线，邮件持久化 = **邮箱永久锁死**（需 GM 删邮件） | `地图类/玩家实例.公会师门.cs:622-690` + `数据类/邮件数据.cs:90-118` |
| R6-11 | `玩家合成灵石` 物品编号未做灵石白名单 → `10×(Y-1)` 合成任意已存在物品 Y | `地图类/玩家实例.cs`（合成灵石） |
| R6-12 | 战功（战令）高级赛季奖励缺 `开启战令` 鉴权 → 未购战令玩家免费领取 | `地图类/玩家实例.cs`（战功领取） |
| R6-13 | 普通账号可对任意在线 30+ 玩家**强制创建师门**（非自愿越归属 + 持久膨胀 + 锁死受害者拜师） | `地图类/玩家实例.公会师门.cs` |
| R6-14 | 账号名大小写折叠冲突（Windows 文件系统不区分大小写）→ 覆盖他人账号文件 / 账号接管 | `账号服务器/主窗口.cs` |
| R6-15 | `高级/替换铭文洗练确认` 随机结果缓冲 `洗练铭文` 未绑源武器、确认不清空不扣料（C08 同型）→ 一次付费结果免费套多把武器 | `地图类/玩家实例.cs:11601-11717` |
| R6-16 | `玩家勋章洗炼` 缓冲 `勋章洗炼/洗炼勋章` 永不复位、无源绑定（C08 同型）→ 一次付费免费套任意/多枚勋章 | `地图类/玩家实例.cs:17956-18063` |

### 🟡 已确认 MEDIUM / LOW（18）

| ID | 级别 | 漏洞 | 位置 |
|----|------|------|------|
| R6-17 † | M | `玩家孔色传承` 缺守卫 → 交易锁定期单方篡改/转移待交易装备孔色（非对称价值窃取） | `地图类/玩家实例.cs:15612` |
| R6-18 | M | NPC 商店备选货币（双货币支付）校验备选币余额却从主货币扣款 → 少付高价值货币 | `地图类/玩家实例.cs` |
| R6-19 | M | 发送邮件冷却字段 `玩家实例.邮件时间` 全程从未赋值 → 唯一限速失效，邮件轰炸/邮件数据无限增长 | `地图类/玩家实例.公会师门.cs:626`，字段 `玩家实例.cs:69` |
| R6-20 | M | 未知暗殿一层（NPC 6350）每日进入上限读后从不回写 → 无限重复进副本 | `Database/System/lua/Npc/NpcFace/6350.lua` |
| R6-21 | M | `玩家铭文刻印` 材料校验用 `&&` 而非 `||`（条件反转）+ 物品编号/铭文索引客户端任选 → 用廉价垫料确定性刻入稀有铭文，绕过洗练 RNG | `地图类/玩家实例.cs:11109` |
| R6-22 | M | 账号失败按账号锁定可被伪造源 IP 放大（per-IP 表与 per-账号表解耦）→ 定向锁死任意账号 | `账号服务器/网络通信.cs` |
| R6-23 | L | 私聊（社交消息 case0）黑名单校验主语写错 → 被拉黑者仍可向拉黑方发私聊 | `地图类/玩家实例.cs` |
| R6-24 | L | 查看他人龙卫 / 查询珍宝商店 整块数据下发无冷却（C15/C16 未覆盖兄弟）→ 出站带宽放大 | `地图类/玩家实例.cs` |
| R6-25 | L | `领取特权礼包`(上期/case2) 误用 `本期特权` 索引奖励表 → 越权领高于实际档位的月卡奖励 | `地图类/玩家实例.cs` |
| R6-26 | L | `购买主题礼包` 物品 B/C/D 客户端自选且无每日已购校验 → 固定元宝重复购买自选最高价值附属物 | `地图类/玩家实例.cs` |
| R6-27 | L | 仓库存取（`玩家转移物品`）只校验与 NPC 坐标距离、漏校验是否同地图 | `地图类/玩家实例.cs` |
| R6-28 | L | 旧版内挂（自动战斗）处理器缺玛法特权校验 → 低权限账号免费开自动战斗 | `网络类/客户网络.cs` |
| R6-29 | L | 认证**成功**路径无限速：成功登录日志 `BeginInvoke` 洪泛（UI 线程内存/卡死）兼 UDP 反射 | `账号服务器/网络通信.cs` |
| R6-30 | L | `玩家铭文刻印` 双材料校验 `&&` → 仅一种材料时对 null 列表 `消耗背包物品` NPE 自断线 + 白白销毁 1000 刻印石 | `地图类/玩家实例.cs:11109`（与 R6-02 同方法） |
| R6-31 | L | `申请发送邮件` 变长包正文偏移越界 `array3[0]`（入口下限写成 94，真实解引用需 98）→ 自连接 DoS + 堆栈日志刷屏（C13 未覆盖兄弟） | `地图类/玩家实例.公会师门.cs:622-649` |
| R6-32 | L | `净化聊天文本` 仅滤 C0/DEL，漏 Unicode 双向覆写(202A-202E/2066-2069)/零宽(200B-200F)/C1/FEFF → 聊天日志(UI+落盘)仍可注入伪装（DEEP-05 未覆盖字符类） | `主程类/主程.日志.cs:50-61` + `启动窗口/主窗口.cs:575-586` |
| R6-33 | L | `净化展示文本`(行会公告/宣言/邮件标题正文/摊位名)漏 C1/零宽/LRM-RLM/2060-2064/BOM（LOW-A 未覆盖字符类） | `地图类/玩家实例.公会师门.cs:1095-1110` |
| R6-34 | L | 账号服务器 `净化日志内容` 刻意保留 `\r\n` → 登录失败路径用账号名换行注入伪造日志行（需多 IP，DEEP-05 同型反向） | `账号服务器/主窗口.cs:120-134` |

### 🔵 存疑（运营场景/多账号相关，建议一并处理）

- **门票仍无 HMAC/时间戳**（PROTO-03/C05 续）：跨机部署时 IP 白名单是唯一认证、UDP 源 IP 可伪造 → 零凭证接管任意账号（含 GM）。单机默认 fail-closed 安全；跨机 + 无 BCP38 时成立。**对应 11.「仍建议后续处理」第一条，应尽快落地门票 HMAC**。
- **`按角色登录-<公开角色名>` 反查**（C05 同型，默认 `专用网关登录器=true`）：知道公开角色名即可伪造门票；受同一来源门控保护，默认不可达，但建议默认关闭该反查。
- **D01 拍卖回归**：`放弃所有拍卖` 用全局计数器 `拍卖序号` 误当活动件序号 → 多件队列时把活动拍品免费判给已退款者。**单账号不可触发**（出价即自动成交），需双账号合谋；底层逻辑确有缺陷应修。`数据类/队伍数据.cs:318`。
- 其它：`行会仓库转出` 单独缺守卫（一致性，真正 dupe 在转入）、`行会变更会员职位` 缺「不得提拔高于本人」上界、限速表满 fail-closed 可被低成本变成全局认证拒服。

### ⛔ 误报 / 驳回（保留以证流程严谨）

- **C01-b「摊位挂单数量与真实堆叠脱钩」**：三视角复核 **0/3 驳回** —— C01 对**堆叠物**的修复实为完整、数量全程守恒。WF1 的该 critical 候选系误报，已剔除（真正的洞是上面的守卫家族 R6-01/05/…）。
- **CRIT-04 `#CALL` 分支未套路径白名单**：代码瑕疵属实（@ 分支修了、#CALL 没修），但触发需 GM 重载 + 先写入爆率目录文件，post-compromise，威胁模型内不可达 → 列入加固 backlog。
- **CRIT-01 绑定器空 `assemblyName` 跳过校验**：远程反序列化路径全部 `TypeNameHandling.None`，绑定器不触发 → 纵深防御缺口，非远程可利用 → 加固 backlog（建议改类型级白名单）。

### 🔭 完整性批判线索（6 轮来未审子系统）

| 优先级 | 未审面 | 风险线索 |
|--------|--------|---------|
| 🔴 高 | **龙卫系统** `哈希监视器` 枚举期 `Remove`（`玩家实例.cs ~14988/15034/15144`） | D01 同型集合改异常 → 远程**扣材料+不发属性+掉线**状态损坏 |
| 🔴 高 | **NpcFace Lua 20 个脚本未审** + 客户端 `选项编号` 无界跳页（`继续Npc对话:7383`/`npc.lua:66`） | 6350.lua 已实证每日上限不回写；系统性逐脚本经济面 |
| 🟡 中 | **副本子系统**（`副本类/*.cs` + `Map/*.lua`） | 层数奖励重复领取 / 首领掉落归属 / 进入扣费每队员校验 |
| 🟡 中 | **月卡** `领取特权礼包` 先清记录位后发奖（`8696-8788`） | 背包满则奖励永久丢失（自伤态损坏） |
| 🟡 中 | **高级狩猎** `刷新狩猎详情`（`16534`）`狩猎编号==0` 免费 re-roll？ | 绕过递增刷新费 |
| 🟢 低 | **天赋突破**（`4519` else 分支）空材料槽免费升级？ | 数据驱动，取决于配置表是否有空突破槽 |

> 批判员已读码确认一批**看守较稳**（交易/邮件附件领取、行会仓库 C02、每日签到、成就领取、悬赏完成、复活/法阵传送、好友/仇人/黑名单 100 上限、龙卫传承重塑先校验后扣），降低这些方向重复挖掘的必要。

### 11.7b WF4 收尾专项结果（6 轮未审子系统 find→verify）

WF4 对上表线索做 7-finder find→verify，**13 候选 → 8 确认 / 0 存疑 / 5 驳回**。确认项多为自伤型 DoS 或经济/逻辑绕过，**未发现跨玩家 dupe/越权**：

| ID | 级别 | 漏洞 | 位置 |
|----|------|------|------|
| R6-35 | 🟡 M | `龙卫全套恢复` 先扣恢复材料(90500×8 / 91212×1)再 `foreach(龙卫属性){ Remove(item) }` 枚举中删自身 HashSet → `InvalidOperationException` → **扣材料 + 强制掉线 + live 集损坏**（D01 同型；`哈希监视器.GetEnumerator` 直返底层迭代器无快照） | `地图类/玩家实例.cs:~15080`（崩溃点 15144） |
| R6-36 | 🟡 L | `龙卫恢复部位` `foreach(龙卫属性.Where(...)){ Remove(item) }` → 同型崩溃强制掉线（不扣材料，纯自伤 DoS） | `地图类/玩家实例.cs:~15005`（15034） |
| R6-37 | 🟡 L | `龙卫记录部位` 同型枚举中删 `哈希监视器` → 崩溃掉线 | `地图类/玩家实例.cs:~14959`（14988） |
| R6-38 | 🟡 M | **对话页面累进无界 + 选项不校验合法性**：`对话页面=对话页面*10+选项编号`，`选项编号` 客户端可控 → 单封包直跳任意 NPC 执行页，绕过页 0 前置门（版本节点/开放时段等） | `地图类/玩家实例.cs` 继续Npc对话(~7383) + `lua/Npc/npc.lua:66` |
| R6-39 | 🟡 L | 未知暗殿二层(6359) 每日进入只校验/记录**队长** → 非队长队员无限次进入绕过每日限制（D03 同型未覆盖兄弟） | `Database/System/lua/Npc/NpcFace/6359.lua` |
| R6-40 | 🟡 M | `升级玛法特权` 缺「同级」守卫 → **1 元宝即可无限续期月卡特权并重置每日礼包领取位** | `地图类/玩家实例.cs`（升级玛法特权） |
| R6-41 | 🟡 L | `领取特权礼包` 先清记录位、`获得月卡奖励` 背包满时提前 `return false` 不回滚 → 当日礼包永久丢失（自伤态损坏） | `地图类/玩家实例.cs:8696-8788` |
| R6-42 | 🟡 L | 高级狩猎完全不校验「最低等级」→ 低级账号可接取/领取高级狩猎获物品与经验 | `地图类/玩家实例.cs`（狩猎） |

**WF4 驳回 5 条**（已读码确认无实际可利用面）：6123.lua 妖塔日限常量未在 Lua 侧定义（门本就形同虚设、非新洞）、6254.lua 对话页 nil 表索引（仅 Lua 运行期错误无危害）、九层妖塔全服共享无日限（设计如此）、狩猎递增金币可被归零编号重置（实测不成立）、开始狩猎覆盖在途（自伤无价值）。

> WF4 修复要点：龙卫三处（R6-35/36/37）枚举源改 `.ToList()` 再 `Remove`（对齐 D01），且 R6-35 应**先重建集合成功、后扣材料**；R6-38 给 `选项编号` 加上界并在 C# 侧校验「该选项在当前页是否合法」；R6-39 校验对象改为当前队员；R6-40 补「目标特权不同于/不低于现有」守卫；R6-41 清位移到发奖成功之后。

### 11.8 统一修复建议

1. **守卫家族（R6-01/02/05/06/07/08/09/17 及 `行会仓库转入/转出`）**：在每个方法入口补
   `if (this.对象死亡 || this.摆摊状态 > 0 || this.交易状态 >= 3) return;`（与 `玩家实例.cs:9382 玩家转移物品`、`升级武器普通:11943`、`玩家合成物品:10417` 等既有写法一致）。逐处核对返回类型（部分返回 `int` 需 `return 0;`）与守卫须置于任何解引用/扣料之前。
2. **根因级纵深防御（强烈建议，可一劳永逸）**：择一 —— ①`放入物品`/`放入摊位物品` 时把物品从 `角色背包` 真正移除（而非仅存引用）；或 ②`查找背包物品`/`消耗背包物品` 跳过"已在交易/摊位"的物品；或 ③`交换物品`/`购买摊位物品` 交付前复核物品仍在 `角色背包` 对应槽位且数量充足。
3. **R6-03/R6-04 另需**：行会仓库转入/转出加守卫的同时，`购买摊位物品` 买断逻辑须按 `能否堆叠` 分支（非堆叠物走"原对象整体转移 + 从卖家删除"，不得按耐久递减）；`828` 行对装备模板应按类型实例化（`new 装备数据`）而非 `new 物品数据`。
4. **R6-02/R6-10 边界**：邮件标题/正文净化后补 UTF8 字节长度上限（标题 ≤60、正文 ≤553）；`消耗背包物品` List 重载补 null 防御。
5. 其余按各条 `notes` 的精确修复点处理；C08 缓冲类（R6-15/16）须"记录源 + `ReferenceEquals` 校验 + 成功/失败两路均清缓冲"。

