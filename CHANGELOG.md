# 更新日志 / Changelog

本仓库遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，日期格式 `YYYY-MM-DD`。

---

## [Unreleased]

### 新增

- **服务端管理窗口新增「GM工具」面板**（左侧「操作」分组，[窗口视图\GMToolView.cs](游戏服务器/窗口视图/GMToolView.cs)）：把原先只能敲文本命令的发放/改装备操作做成图形界面，分两页。**发放页**：填角色名，选元宝/金币/物品/装备/邮件物品，可搜索物品名（数据源 `游戏物品.检索表`）、填数量、勾绑定，一键发放——直接复用现成 GM 命令（`添加元宝`/`添加金币`/`添加物品`/`制造物品`），邮件走 `角色数据.发送邮件`，在线离线通用。**装备页**：填角色名「加载装备」，把该角色穿戴+背包里的装备以快照列进表格（强化/幸运/绑定/随机属性/精炼孔洞概要），点选一行即可在右侧编辑并应用：① 强化等级（按职业同步升级攻/魔/道/刺/弓）、幸运/品质、当前/最大持久、绑定；② 随机属性词条（从 `随机属性.数据表` 选最多 4 条，留「（无）」即清除）；③ 精炼阶段（0-3，对齐官方「最多 3 条精炼属性」）/精炼值一二三/精炼次数、孔数（0-3，对齐官方「最多 3 孔」）与各孔颜色（官方 7 色红黄蓝绿紫灰橙 + 本引擎扩展褐/多彩，已去「无孔」）、各孔镶嵌灵石（下拉仅 `灵石配置` 合法灵石，应用时按游戏 `玩家镶嵌灵石` 的同名同色规则校验灵石与孔色匹配、孔位须 < 孔数，不匹配即跳过记日志）。在线改完即时下发 `玩家物品变动` 刷新客户端，离线改完经脏标记落库、登录后生效。
  - **线程安全**：新增 [管理命令\委托命令.cs](游戏服务器/管理命令/委托命令.cs)（`GM命令` 的 `Action` 载体）。UI 不直接碰 `角色数据`/`装备数据`（避免与服务循环竞态、`字典监视器`/`列表监视器` 并发读抛「集合已修改」），所有读写都包成闭包丢进 `主程.外部命令` 队列、由主循环单线程执行；读装备先在游戏线程拷成纯值快照再回填 UI，UI 永不持有 live 对象。改装备的定位/同步范式照搬现有 `修改升级次数`/`修改幸运等级` 命令。导航接入仿 `navBarItem15`（[SMain.cs](游戏服务器/启动窗口/SMain.cs)、[SMain.Designer.cs](游戏服务器/启动窗口/SMain.Designer.cs)）。

### 变更

- **启动数据加载并行化**：海量模板小文件（技能 5581 / 物品 4445 / NPC 4215 / 地图 2695 等约 1.9 万个 `.txt`）原先在 `序列化类.反序列化` 里逐文件串行 `File.ReadAllText` + 多轮整串 `.Replace` + `JsonConvert`，是启动慢的主因之一。两个 `反序列化` 重载的内层循环改为 `Parallel.ForEach`：非泛型重载本就用线程安全的 `ConcurrentQueue`；泛型重载把非线程安全的 `List` 换成 `ConcurrentQueue`，并补齐逐文件 `try/catch`+记日志（单个损坏文件不再中断整批加载）。多核下接近线性加速；`数据表` 写入仍在 `反序列化` 返回之后单线程进行，无竞态。代价：返回数组顺序不再确定（现有调用方均按对象自身字段建索引，不依赖顺序）
  - **怪物爆率二次 IO 并行化**：`游戏怪物.载入数据` 反序列化完 2323 个怪物文件（`Npc数据/怪物数据/`）后，原本还有一个串行 `for` 逐怪物 `加载怪物爆率`（`File.OpenText` 逐行解析，含 `#CALL`/`@` 递归再开文件），Lever A 未覆盖。每个怪物只写自己的 `怪物掉落物品`、彼此独立，改为 `Parallel.ForEach` 并行加载（每怪物一次、爆率文件存在才打开，`怪物爆率/` 共 582 个文件 + `#CALL`/`@` 递归）；`数据表.Add` 保留在并行之后单线程执行
  - **快速 `Point` 转换器（点集解析提速的关键）**：耗时探针显示瓶颈不在文件数而在文件体积——`地图区域`（164 文件 / 56MB）与 `怪物刷新`（515 文件 / 85MB）合计 141MB，主体是 `范围坐标` 里数百万个 `"x, y"` 坐标点。Newtonsoft 默认走 `System.Drawing.PointConverter`（文化相关解析，慢）。新增 `快速坐标转换器 : JsonConverter<Point>`（`Split`+`int.TryParse(ReadOnlySpan)` 直解，写回仍为 `"x, y"`、与既有文件格式兼容，并兼容 `{"X":..,"Y":..}` 对象形式），注册进 `全局设置`，直接提速这数百万个点的反序列化
  - **跳过无谓的整串替换**：`序列化类.反序列化` 两个重载在替换前先判定 `$type` / `Assembly-CSharp` 是否存在，不含旧标记的纯坐标点集文件跳过全部 18 轮 `.Replace`；含 `$type` 的技能节点文件照常替换（修正了首版仅判 `Assembly-CSharp` 漏掉技能短类型名导致的 `Error resolving type`）。注：.NET 的 `string.Replace` 在无匹配时返回原串、不分配内存，故此项本身收益有限，主要是省去对超大文件的重复扫描
  - **巨型文件负载均衡**：`地图区域` / `怪物刷新` 目录里混着 3.4MB 巨型文件与大量小文件，默认 `Parallel.ForEach` 分区可能把大文件压在一个核上拖成长尾。`序列化类.反序列化` 两个重载改为先按体积降序排序、再用 `Partitioner.Create(files, loadBalance: true)` 动态分区，大文件优先被领取、小文件填满空闲核
  - **加启动耗时探针**：`系统数据网关` 每个 loader 的日志追加 `耗时 Xms` 并在末尾打印总耗时；`SMain` 启动加载三阶段（系统数据 / 脚本系统 / 客户数据）各打印耗时与总耗时，便于定位剩余瓶颈
  - `系统数据网关` 的 58 个 loader 外层**仍保持串行**：存在 4 条加载期跨类型依赖（`传送法阵`→`游戏地图` / `装备属性`→`随机属性` / `游戏套装`→`游戏物品` / `地图道具`→`游戏怪物`），整批 `Parallel.ForEach`（已注释的 `LoadData`）会撞 NRE / 集合已修改，故当年退回串行是正确的，本次未动
- **游戏登录器改回 net48 + 重做界面皮肤**：原登录器是 net8 单文件发布，玩家端需预装 .NET 8 桌面运行时；现 `游戏登录器.csproj` 目标框架改回 `net48`（Win10/11 自带运行时，玩家双击即用、免装），`x86`、`DebugType=none`，并加 `Microsoft.NETFramework.ReferenceAssemblies` 使无 net48 目标包的机器也能用 dotnet SDK 编译、加 `System.Resources.Extensions` + `GenerateResourceUsePreserializedResources` 让新 SDK 能处理 resx 里的二进制图标资源。产物为 `Release\net48\` 目录（exe + 8 个依赖 dll，net48 应用常规以文件夹分发）
  - **net48 缺失的 .NET 5+ API 等价补齐**（新增 `兼容随机.cs`，安全行为不变）：`SHA256.HashData` → `SHA256.Create().ComputeHash`；`RandomNumberGenerator.GetInt32` → 拒绝采样消除取模偏置的等价实现（`登录界面.cs` 包号、`图形验证.cs` 验证码各处）；`ProcessStartInfo.ArgumentList`（net48 无）→ 按 Windows `CommandLineToArgvW` 规则手工转义后写 `Arguments`，复刻自动转义、保留「区服名/票据含特殊字符也无法破出命令行参数边界」的纵深防御
  - **界面皮肤重做**（仅换外壳，网络协议/安全逻辑/控件名/事件全部沿用，`登录界面.cs` 业务逻辑零改动）：`FormBorderStyle` 改无边框 + `GraphicsPath` 圆角裁剪 + Win32 `WM_NCLBUTTONDOWN` 拖动；新增自绘标题栏（最小化/关闭深色按钮，关闭按钮 X 直接彻底退出登录器、不再收进托盘）；图标换为传奇风 `app.ico`（`ApplicationIcon` + 构造时 `ExtractAssociatedIcon` 运行时覆盖 resx 旧图标，任务栏/Alt-Tab/托盘同步）；`UITabControl` 居中、顶部金色选项卡导航（选中金色下划线）；「启动游戏」页签改为**登录后才出现**（启动时从导航移除，登录成功 `case "0"` 再 `TabPages.Add` 并选中、注销时移除），并清掉 Designer 里的设计期假占位（账号 `mistyes`、区服 `魔龙谷/伤心树`），未登录时只显示 账号登录/账号注册/密码修改 三页、无残留数据；`AutoScaleMode` 由 `Font` 改 `None`，避免字体自动缩放把窗体宽高按不同比例拉伸导致立绘失真，窗体固定 900×556；配色由奶油红粉整体改为传奇风深木金/古铜（金边登录卡片 + 白底金框输入框 + 金色按钮）；立绘背景沿用 `Resources\登录器.png`（可直接替换该图换皮）
- **登录器自动生成默认配置**：`ServerCfg.txt` 缺失时不再直接报错退出，而是自动生成默认单机配置 `127.0.0.1:8001`（本机账号服务器默认监听端口）并提示，单机开箱即用；联网改该文件后重启即可。沿用账号服务器 `Server.txt` 缺失自动创建的同款风格（`登录界面.cs`）
- **移除游戏客户端启动前的签名校验**：原 `校验游戏可执行文件`（Authenticode 弱模式）对未用受信证书签名的客户端会弹「可能已被篡改」警告，导致每个玩家每次进游戏都被吓一跳。已删除该方法及其调用与 `X509Certificates` 引用，进入游戏直接启动（`登录界面.cs`）。代价：丢失「防本地游戏 exe 被篡改」这一弱防护，对以管理员权限运行的登录器影响有限
- **移除「九八/百牛专用网关 ⇌ 通用账号网关」切换开关，固定走项目自带的通用账号网关**：全局设置「网关」框里那个 `专用网关登录器check` 勾选项（勾上＝九八专用门票、不勾＝通用门票）对自带账号服务器无意义——本仓库 `账号服务器` 发的门票恒为 2 段 `门票号;账号`（通用格式），一旦误勾成专用模式，`网络服务网关.处理数据` 会按 5 段解析、段数 `!= 5` 全部丢弃，导致全服无人能登录，是个只会帮倒忙的坑。改动：① `网络服务网关.cs` 删掉 `if (Settings.专用网关登录器){九八5段分支} else {通用2段分支}`，固定只跑通用 2 段门票解析，并一并清掉同方法内一大段早已注释禁用的「九八百牛 / http 门票」死代码；② `Settings.cs` 删字段 `专用网关登录器` 及其 ini 读/写（旧 `Setup.ini` 里残留的 `专用网关登录器=` 键被忽略，无害）；③ `ConfigInfoView.cs/.Designer.cs` 把全局设置里整个「网关」组框（`groupControl2` + 勾选框 + 那行警示语 `labelControl25`）连同其 `Controls.Add` / `BeginInit` / `EndInit` / 字段声明全部移除，界面上不再保留该框。`PROTO-03` 门票来源 IP 白名单校验原样保留。代价：放弃九八专用门票里的「按角色名登录」「UUID / 推广代码 / 推荐人码」字段，本就只服务于九八账号网关、与自带账号体系无关

### 修复

- **所有杀怪成就点「领取」都无反应（死锁判断致永久领不了）**（[issue #4](https://github.com/awp0721/CQYH_Server/issues/4)）：`玩家实例.领取杀怪成就` 的领取条件里有一条 `this.角色数据.杀怪领取.ContainsKey(成就编号)`，但 `杀怪领取` 这个 `字典监视器` **全仓只有一个写入点——领取成功分支自己**（`杀怪领取[成就编号] |= (byte)(1<<进度编号)`），建号 / 登录 / 别处从不为它预填 key。于是构成鸡生蛋死锁：要进领取分支必须该 key 已存在 → 而 key 只有进了领取分支才会被创建 → **首次领取条件恒假，永远进不去，所有成就点击领取都毫无反应**。而这条 `ContainsKey` 本就多余——`字典监视器` 索引器对缺失 key 安全返回 `default`（`byte 0`），紧跟的 `(杀怪领取[成就编号] & (1<<进度编号)) == 0` 用 `0 & bit == 0` 已能正确表达「该档未领取」。修复：从领取条件中删除 `杀怪领取.ContainsKey(成就编号)` 一段（保留 `杀怪成就.ContainsKey` 确保有击杀进度），首次领取即可正常发放成就点数 / 经验并回执。经核对计数链路（`怪物实例.cs` 同时按 `怪物编号` 与 `分组编号` 匹配）与配置（`杀怪成就.csv` 稻草人 QuestID=1、稻草人怪物模板 `分组编号=10003`）均无问题，纯属领取判断挡死（`玩家实例.cs`）
- **未配 `道具列表` 的宝箱一开就崩、连带全图宝箱都打不开**（[issue #2](https://github.com/awp0721/CQYH_Server/issues/2)）：`玩家实例.FilterItemTreasures(items)` 用 `from x in items?.Where(...) orderby ... select x` 过滤宝箱掉落，作者本想用 `?.` 防 `items` 为空，但只防了一半——`items?.Where(...)` 在 `items == null` 时整体返回 `null`，紧接着的 `orderby`（即 `Enumerable.OrderBy`）拿到 `null` 抛 `ArgumentNullException: Value cannot be null. (Parameter 'source')`。调用方 `道具实例.处理对象数据` 把 `this.Template.道具列表` 原样传入，**只要某个宝箱模板没配 `道具列表` 该字段就是 null**，玩家一开此箱即抛异常，且异常一路冒泡到 `主程.服务循环` 被记为「致命错误」、当帧地图处理中断，表现为出生地箱子 / 任务箱子等全部「点了没反应」。修复：`FilterItemTreasures` 开头判 `items == null` 时直接返回 `Array.Empty<游戏宝箱>()`，并把 `items?.Where` 改回 `items.Where`，null 宝箱模板只是不产出 `道具列表` 掉落（其 `掉落模板` 分支在调用处另行处理、不受影响），不再崩服（`玩家实例.cs`）
- **成长配置坏数据致服务器启动崩溃且无从定位**：`角色成长` 的静态构造函数解析 `System\玩家升级经验.txt` / `玩家升级战力.txt` 时，对每行 `=` 右侧直接 `long.Parse` / `int.Parse`，`Trim()` 只去首尾空白去不掉数字中间的空格。某行值被手误打成 `10 2`（本该 `102`）时 `long.Parse("10 2")` 抛 `FormatException`；因在 `static cctor` 中抛出，被包成 `TypeInitializationException` 经 `玩家实例..ctor`→`机器人.初始化`→`服务循环` 一路冒泡为未处理异常，服务器直接起不来，而异常信息只甩一个 `'10 2'`，既不说是哪个文件也不说是哪一行。修复：抽出 `加载键值文件` 统一逐行解析两个 `键=值` 文件，跳过空行、缺 `=` 即报错，并对每行解析包 `try/catch`，失败时抛出带 **文件名 + 行号 + 原始行内容 + 内层报错** 的 `InvalidDataException`（`成长属性.txt` 的逐行解析同样补上行级 try/catch）；定位由「大海捞针」变为一眼可见。未做静默去空格——`10 2` 该是 `102` 还是别的无法臆断，与其猜错污染游戏数据不如响亮报错（`角色成长.cs`）
- **LF 换行的成长配置文件被整份当成「第 1 行」致服务器启动崩溃**：`角色成长` 静态构造函数用 `Regex.Split(..., "\r\n")` 按**字面量 CRLF** 切行解析 `成长属性.txt` / `玩家升级经验.txt` / `玩家升级战力.txt`。当数据文件是 LF（`\n`）换行（编辑器另存、跨平台传输等很常见）时文中无 `\r\n` 序列，`Split` 切不开，**整个文件被当作一行**，于是 `值 = "10\n2\n3\n20…"`、`long.Parse` 抛 `FormatException`，在 `static cctor` 中被包成 `TypeInitializationException` 冒泡为未处理异常，服务器起不来；错误框「第 1 行」后跟着整份文件内容正是此症状的铁证。修复：两处切行的分隔符由 `"\r\n"` 改为 `"\r\n|\r|\n"`，对 CRLF / LF / 单 CR 三种换行均正确切分且行尾不残留 `\r`（`成长属性.txt` 按 `\t` 分列也因此更稳）。本仓库自带的数据文件本是 CRLF、不受影响，此改为公开引擎用户的 LF 数据文件兜底（`角色成长.cs`）
- **关服点「X」卡死 / 未响应、关服要等 20 秒以上**：游戏服务器 `服务循环` 的收尾段（`主程.循环.cs`）写死了两段 `Thread.Sleep(10000)`（清理物品后一次、强制保存后一次），与全量 `强制保存`+`导出数据`（序列化落盘 + GZip 备份）叠加，关一次服最少干等 20 秒；而当前在用的 `SMain.OnClosing` 在 UI 线程里自旋 `while (主程.主线程 != null)` 等收尾跑完，期间窗口无法重绘、被 Windows 标记「未响应」，表现为"卡住"。把两段 `Thread.Sleep(10000)` 各改为 `Thread.Sleep(500)`（其前后的清理/保存/导出均为同步调用、返回即已完成，长睡眠纯属冗余保险），关服时长由 20s+ 降到约 1s + 真实落盘耗时；同时把收尾末行日志改为输出 `服务器已成功关闭, 关服保存耗时:X.X 秒`，方便直接从系统日志判断落盘真实开销
- **关服收尾与后台定时自动保存的写盘竞态**：定时自动保存 `保存数据库()`（`主程.服务.cs`）走 `Task.Run` 在后台执行 `导出数据()`，期间 `自动保存中=true`；若关服恰在该后台任务进行中触发，主线程收尾段的 `清理物品 / 强制保存 / 导出数据` 会与之并发，可致 ① 后台遍历数据表时撞 `清理物品` 的删改（集合已修改异常）② 两路并发写同一对象的 `MemoryStream` 损坏序列化 ③ 两个 `导出数据` 并发 `File.Create/Move` 同一批数据文件而崩溃。修复：收尾段开头新增 `while (主程.自动保存中)` 等待（含 30s 超时兜底 + 超时日志），等当前这次后台自动保存落盘完成再继续；此时主循环已退出、不会再触发新的自动保存，故只需等这一个，正常无后台保存在跑时该循环零开销（`主程.循环.cs`）
- **`游戏商店` 加载缓慢（84 个商店文件 / 669KB 却耗时约 350ms）**：`游戏商店.载入数据` 把整段固定字节的 CRC32 放进 `for (i < array.Length)` 循环算了「字节数」次、累加进会溢出且最终被覆盖的 `num`，构成 O(n²)（`游戏商店.cs`）。改为单次 `计算类.CRC(array)`；`商店文件效验` 仅作客户端缓存版本令牌（`玩家实例.cs:8028` 回传比对、客户端不自算 CRC），改后部署首次开商店重新同步一次即稳定、无害。该 loader 由约 350ms 降到个位数 ms
- **登录卡死("点登录没反应")**：账号服务器登录成功响应字段数与客户端不一致，导致登录后界面不跳转、无任何提示
  - 根因：MED-E/PROTO-01「登录响应不再回显密码哈希」只改了客户端（`登录界面.cs` case "0" 改为严格校验 `array.Length==4`），账号服务器 `网络通信.cs:279` 的注释虽已加上「不再回显」，但代码仍把 `array[3]`（密码哈希）拼进响应，实际发出 5 段 `包号 0 账号 哈希 区服列表`。客户端收到 5 段 `!= 4` → 静默 break → 界面停滞
  - 修复：账号服务器登录响应删除多余的密码哈希字段，固定为 4 段 `包号 0 账号 区服列表`，与客户端对齐（同时兑现 PROTO-01 不回显哈希的安全意图）。注册/改密/门票三类响应经核对字段数本就一致，无需改动
- **登录器请求超时无提示**：`ServerCfg.txt` IP/端口错误或账号服务器未响应时，30s 超时计时器只静默解锁界面，表现为"点了没反应"。改为弹窗提示「连接服务器超时」并指引检查服务器与配置（`登录界面.cs` 新增 `登录请求超时`，替换 `用户界面计时.Tick` 原绑定的 `用户界面解锁`）
- **中文 / 非 ASCII 安装路径下 Lua 脚本加载失败**：服务端放在含中文的目录（如 `公开引擎`、`游戏服务器`）时，`require "init"` 等报 `module 'init' not found`，且报错里的中文路径显示为 `������` 乱码方块
  - 根因：NLua 把含中文的 `package.path` 按 UTF-8 交给 Lua，而 Lua 5.4 原生 `require` 底层用 C 运行库 `fopen` 按系统 ANSI 代码页（简体中文为 GBK/936）打开文件，无法识别 UTF-8 字节序列的中文路径
  - 修复：`游戏脚本.cs` 新增静态方法 `加载lua模块` + 一个走 .NET `File.ReadAllText` 的 Lua 模块搜索器（插入 `package.searchers` 第 2 位，优先于默认 path 搜索器）。读文件完全在托管侧完成、只把源码交给 `load`，彻底绕开原生 `fopen`；入口 `main.lua` 同步改用 `File.ReadAllText` + `DoString` 加载。中文与纯英文路径均可正常加载
- **中文变量名 Lua 脚本无法编译（魔改 Lua 未接入构建）**：脚本系统大量使用中文标识符（`主程`、`计算类` 等），标准 Lua 5.4 词法分析器不接受非 ASCII 标识符，修好上面的路径问题后 `init.lua` 暴露出 `unexpected symbol near '<\228>'`
  - 根因：作者准备的、支持中文标识符的魔改 `lua54.dll` 一直留在 `游戏服务器/` 根目录，但从未接入构建；KeraLua（NLua 依赖）始终加载 NuGet 自带的标准 Lua（`runtimes/win-x64/native/lua54.dll`）
  - 修复：`游戏服务器.csproj` 新增 `AfterTargets="Build"` 与 `"Publish"` 的 MSBuild Target，构建/发布后自动用根目录的魔改 `lua54.dll` 覆盖输出根目录与 `runtimes/win-x64/native/` 下的标准版。`clone` + `build` 即自动启用，无需手动替换 dll
- **C07 安全守卫回归：短定长封包被一律踢线（玩家几乎寸步难行）**：第五轮 C07 为堵「变长封包总长字段填 0/1/2/3 致 `取封包` 游标零推进、接收回调 `while(true)` 无限重解析」的预鉴权 DoS，加了 `if (value2 < 4) throw` 断连守卫。但该检查被放在 `value2 = (变长 ? 读头部总长 : 注册表长度)` 这条合并语句**之后**，于是把**所有注册长度 < 4 的定长包**也一并误杀——而长度 2/3 的客户端定长包遍地都是：`查询地图路线`(530)、`打开角色背包`(598)、`玩家退出登录`(183)、`玩家每日签到`(191)、交易确认/锁定/解锁/结束(151~154)、摆摊系列(103/105/106) 等数十个核心操作。任一这类包经 [客户网络.cs](游戏服务器/网络类/客户网络.cs) 的 `catch → 尝试断开连接(e)` 直接踢线，表现为玩家点地图/开背包/签到即掉线（系统日志刷 `封包总长非法(变长封包头部不足4字节)! 封包编号:530 总长:2`）。修复：把 `< 4` 检查收进**变长分支内部**（仅当注册 `长度 == 0` 才读头部并校验），定长包 `value2` 取自可信注册表、游标必 `Skip(value2≥1)` 推进、不存在死循环，故不再被波及；变长包的 DoS 防护原样保留（`游戏封包.cs` `取封包`）

### 安全修复（第二轮深度审计 2026-05-27）

| ID | 严重 | 修复 |
|----|------|------|
| PROTO-04 续 | 🟠 | 7 处封包处理器（鉴定无相钥石 / 内挂物品过滤 / 领取七天奖励 / 领取七天大奖 / 验证动态密码 / 组队拍卖竞价 / 玩家放弃任务）补加阶段守卫 |
| DEEP-04 | 🟠 | 自定义扩展封包：鉴权 + 字段 1KB 上限 + 日志注入控制字符过滤 |
| DEEP-05 | 🟠 | 聊天日志注入：`主程.添加聊天日志` 与旧版 `主窗口.添加聊天日志` 都加入文本净化 |
| DEEP-06 | 🟠 | 聊天封包带宽放大：发送聊天信息 / 发送好友聊天 / 发送社交消息 单条上限收紧到 2KB |
| DEEP-07 | 🟡 | WebApi：HTTP body 64KB 上限 + 异常信息不再回显客户端 + null 反序列化防御 |
| HIGH-04 | 🟠 | 商人发货命令: `元宝数量*100` 整数溢出 + ulong 累加防 uint 回绕, 防对头利用商人对受害者元宝执行归零攻击 (同步加固赠送元宝) |
| MISC-06 | 🟡 | 玩家合成物品: 原 `_ = this.交易状态;` 死代码守卫替换为真实的死亡/摆摊/交易状态检查, 防止异常状态下合成产生不一致 |
| HIGH-05 | 🟠 | 队伍队长强制踢人未清理被踢者的组队竞拍, 导致竞价金币锁死无法通过邮件退回; 补齐 `放弃所有拍卖` 调用 |
| LOW-A | 🟡 | 行会公告/宣言/摊位名字 新增控制字符与 Unicode 双向覆写过滤 (`净化展示文本`), 防止钓鱼伪装系统通知 |

详见 [SECURITY_AUDIT.md](SECURITY_AUDIT.md#七第二轮深度审计补充2026-05-27) 与第八节第三轮针对性审计。三个项目 `dotnet build` 仍为 0 errors。

### 安全修复（第五轮 多代理工作流审计 2026-05-30）

本轮用多代理工作流做远程攻击面专项审查（18 个 finder 多模态扫描 → 53 候选 → 每条候选"可达性 + 利用机制"双重对抗验证 → 综合裁决），再对每条确认项做逐条深读复核与精确修复规格（25 条 `real-fix-now` + 1 条误报）。范围限定**远程可达**漏洞（攻击者仅能收发封包、至多持普通账号），排除需 GM / 已知受害者密码 / 写服务器文件 / 本地访问 的 post-compromise 问题。全部 25 条已修复，三项目 `dotnet build` 0 errors / 0 warnings。

| ID | 严重 | 修复 |
|----|------|------|
| C05 | 🔴 | 门票来源白名单改 **fail-closed**（未配置时仅放行环回，远程来源一律拒绝）+ 修 `Settings` 专用网关 fallback 错值；杜绝默认部署下任意 IP 用 UDP 注入伪造门票零凭证接管任意账号（含 GM） |
| C01 | 🟠 | 摆摊买断分支按真实堆叠 `当前持久` 递减、买家恒收一个 `购买数量` 的新建子对象；修"挂单数量 < 真实堆叠时买断整堆物品被复制交付且销毁卖家余量"的价值窃取 |
| C02 | 🟠 | `更改存储权限` 封包补会长职位鉴权 + 标志位 `& 0x0FFF` 裁剪；防任意会员自授行会仓库取出权限盗空公会仓库 / 清空会长权限 DoS |
| C03 | 🟠 | `玩家合成装备` 合成参数按真实背包槽位累计去重校验+消耗；防同一物品填多个配方位"一份材料产整件" |
| C04 | 🟠 | `组队拍卖竞价` 校验客户端 `拍卖序号` 必为当前活动件序号；防越序竞价劫持他人中标物品并吞掉受害者出价 |
| C06 | 🟠 | 账号服认证限速表满改 **fail-closed**（认证类请求返回恒封禁哨兵桶）+ 新增**按账号维度**的失败封禁（20 次/5min→封 5min）；防伪造源 IP 撑爆 per-IP 表后全局解锁口令/密保暴破 |
| C07 | 🟠 | 变长封包总长 `< 4` 直接抛异常断连；修零字段封包（`UnknownC644`/`敏感字符过滤`，总长填 0）令 `取封包` 游标零推进、接收回调 `while(true)` 无限重解析的预鉴权远程 DoS（CPU 钉死 + 接收列表 OOM） |
| D01 | 🟠 | `放弃所有拍卖` 改快照键迭代 + `TryGetValue` 重取 + `MaxBy` 空序列守卫；修 foreach 枚举活字典时 `拍卖分配物品` 内 `Remove` 致"集合已修改"异常 / 空序列 NRE（主循环线程路径会跳过整 tick）；同步修 `放弃拍卖` 同型 NRE |
| C08 | 🟡 | `装备精炼替换` 把临时精炼缓冲绑定到产生它的源装备（`ReferenceEquals`）并要求目标 `开启精炼 > 0`；防用廉价垫件刷出结果后跨装备/跨分类套用属性绕开开槽材料成本 |
| C09 | 🟡 | `放入摊位物品` 拦截灵魂绑定装备（与交易/寄售/邮件/师门路径一致）；防 +9 灵魂绑定装备经摆摊跨账号套现绕过禁卖约束 |
| C10 | 🟡 | 账号注册新增账号总数硬上限（默认 100 万）；防伪造源 IP 绕过 per-IP 注册限速持续灌文件耗尽磁盘/inode |
| C11 | 🟡 | `门票数据表` 周期清扫过期项 + 2 万条硬上限止血；防只喷 UDP 不发 TCP 致门票字典无界增长 OOM |
| C12 | 🟡 | `限制重要封包间隔时间` 默认改为开启（放大类处理器节流默认生效，运营仍可在 UI 关闭） |
| C13 | ⚪ | `装备雕色` 补 `孔洞颜色`(0..8)/`孔洞位置`(0≤x<Count) 边界校验，修 off-by-one + 负数放行；防越界异常自连接 DoS |
| C14 | ⚪ | `玩家打开摊位`/`购买摊位物品` 补同地图 + 网格距离(≤12)校验，与 `玩家申请交易` 对齐；防跨地图远程扫描/买空任意在线摊位 |
| C15 | ⚪ | `查看对象装备` 加 300ms 无条件冷却（单封包放大为全装备+天赋 4 个序列化下发包）；防属性能退化型 DoS |
| C16 | ⚪ | 寄售三查询（我的/筛选/指定）加 500ms 无条件冷却（每次请求全表扫描）；防算法型 DoS |
| C17 | ⚪ | `特权引导寻路` 补阶段守卫；防登录前阶段 0 探测守卫 NPC 坐标并诱发查表/回包 |
| C18 | ⚪ | 挖矿每 tick 复核当前地图，离开矿区（传送/换图）即停挖；防离图后续发矿白嫖 |
| C19 | ⚪ | 移除以客户端自报 MAC（可伪造）作网卡封禁键的登录闸口，登录改用权威 socket IP（对齐 PROTO-08）；防伪造 MAC 绕过封禁/嫁祸 |
| C20 | ⚪ | 主题礼包周末日期判断 `< 1 && > 5`（恒假）改为 `||` + 查表失败即拒的纵深防御；修周末任意日期序号被放行越权获取任意装备 |
| C22 | ⚪ | 入会申请审批补复检行会 100 人上限；防官员批准使成员突破上限致计数 `(byte)` 回绕 + 描述广播 O(N²) 放大 |
| C23 | ⚪ | NPC 6361 付费传送的 `else` 分支补回扣费（原只判金币不扣）；修副本内无限免费传送（金币 sink 失效，Lua） |
| D02 | 🟡 | 挖矿入口校验武器（0 号位）存在且耐久 > 0；防空武器栏使 `武器损失持久` 取不到武器从而零成本无损耗刷矿 |
| D03 | ⚪ | NPC 6112 妖塔组队进入：金币前置校验对象由队长改回当前各队员（原误用队长）；防金币不足 10 万的小号被免费传送进副本（Lua） |

> 误报（已读码确认，不修）：C21（`游戏封包.string` 字段 `Split` 取首元素空内容越界）—— 实际 `Split(new char[1], StringSplitOptions.RemoveEmptyEntries)` 全为分隔/空时返回空数组配 `[0]` 才越界，但该重载在无分隔符时返回含原串的单元素数组，空串返回空数组取 `[0]` 确会越界——经核：调用方均带定长字段、`描述符.长度` 为 0 时读空串的路径不可达，判误报。

详见 [SECURITY_AUDIT.md](SECURITY_AUDIT.md) 第十节。

### 其他

- README 新增 QQ 技术交流群 `1105229144`,明确标注**仅限单机技术交流**,禁止商业运营/私服推广/外挂相关话题;同时在「联系方式」节列出 Issues / 邮箱 / QQ 群三种渠道
- 补齐 0.6.0 漏改的 SMain(新主窗口)品牌:`（传奇永恒）游戏服务端 - 游戏区名称：` → `Elaina Engine - 游戏区名称：`
- 服务器启动时在系统日志输出 banner:`Elaina Engine (伊蕾娜引擎)` + `源码仓库: https://github.com/awp0721/CQYH_Server`
  - 注意:`账号服务器/主窗口.cs` 中默认 Server.txt 的 `/传奇永恒` 分组名暂未修改,因其影响客户端登录时的分组匹配,改名需配合客户端预设同步
- 移除游戏服务器中残留的"软件授权"机制
  - 删除 `游戏服务器/LicenseTool/`(`LicenseInfo.cs` + `LicenseLoader.cs`,含硬编码 RSA 私钥 + Win32_Processor 机器码方案)
  - 清理 `Program.cs` 启动时早已注释的 LicenseLoader 死代码块,简化 Main 入口
  - 移除 `主窗口.加载系统数据()` 里"授权状态" / "本机机器码"系统日志输出
  - 重命名遗留的 `S_软件授权分组` 控件 → `S_充值密钥分组`(Text 在更早提交里已改为"充值平台密钥")
  - 删除 `SMain.cs` / `主窗口.cs` 顶部的 `//using LicenseTool;` 注释残留
- 规范化 `游戏服务器/` 根目录散落文件,统一按"\*\*类 / \*\*窗口"风格归类
  - 新建 `主程类/`:收纳 6 个 `主程.*.cs` 分部类
  - 新建 `启动窗口/`:收纳 `SMain` / `SMainR` / `主窗口` 三套窗体文件(含 `.Designer.cs` / `.resx`)
  - 合并 `AStar/` + `AStarPathing/` → `寻路类/`(10 个寻路相关文件)
  - 保留 `Program.cs` / `Settings.cs` / `GlobalUsings.cs` / `App.config` / `app.ico` / `lua54.dll` / `csproj` 等入口/配置文件在根目录
- 新增 `Database/README.md`:说明引擎基础数据目录结构、部署放置位置、`Settings.游戏数据目录` 配置方式、典型访问路径
  - 顺带消除 GitHub 仓库首页 `Database/System` 单子目录折叠显示

---

## [0.6.0] - 2026-05-27

- 配套 `Database/` 引擎基础数据加入仓库：地图 / 物品 / 技能 / NPC / 触发器 / csv 配置，约 19,000 文件
- 三个窗口标题统一为 **Elaina Engine** 品牌
  - 游戏服务器主窗口: `九八游戏服务器` → `Elaina Engine`
  - 账号服务器: `永恒传奇登登录服务器`（原始错字"登登"） → `Elaina Engine - 账号服务器`
  - 游戏登录器: `永恒传奇登录器` → `Elaina Engine - 登录器`

---

## [0.5.0] - 2026-05-24

- 拆分 `玩家实例.cs`：24,901 → 19,602 行，抽出 4 个 partial 文件（挖矿 / 公会师门 / 交易摆摊 / 自动挂机）
- README 顶部加入 Elaina 立绘 + Elaina Pro 商用引流章节
- 新增 LICENSE（仅供学习/非商用自定义协议）、CONTRIBUTING.md、GitHub Issue / PR 模板
- 新增 `.gitattributes` 规范化行尾，校正 README 技术栈描述
- csproj `NoWarn` 抑制反编译产物噪音：57 warnings → 3
- 文档措辞中性化

---

## [0.4.0] - 2026-05-24

- 引擎命名为 **Elaina (伊蕾娜)**：README 标题 + AssemblyInfo 同步
- 清除 AssemblyInfo 中的原始厂商元数据

---

## [0.3.0] - 2026-05-24

- 扁平化 `游戏服务器/游戏服务器/` 双层目录结构
- `主程.cs` 按功能拆分为 6 个 partial 文件（1315 → 188 行）

---

## [0.2.0] - 2026-05-23

- 60+ 散落 .cs 文件按功能归位（新建 `日志类/` `任务类/`，合并外层 `工具类` `窗口视图`）
- 标识符可读性优化：`_0015_...` / `_0008_0006_...` → `WebApi` / `AutoBattle`
- 删除死代码：`------------/` 文件夹、`Attribute0/1/2.cs`、`Form1.cs`、`lua54.zip`、孤立 sln、重复 enum
- 新增 `GlobalUsings.cs`（.NET 8 全局 using）

---

## [0.1.0] - 2026-05-23

### 安全修复

详细审计报告见 [SECURITY_AUDIT.md](SECURITY_AUDIT.md)。

| ID | 严重 | 修复 |
|----|------|------|
| CRIT-01 | 🔴 | Newtonsoft.Json TypeNameHandling RCE → SerializationBinder 白名单 |
| CRIT-02 | 🔴 | 门票 RNG 可预测 → `RandomNumberGenerator` |
| CRIT-03 | 🔴 | 账号文件路径穿越 → 字符白名单 + 路径规范化 |
| CRIT-04 | 🔴 | 怪物爆率文件路径穿越 → 同上 |
| HIGH-01 | 🟠 | IP 封禁绕过（缺 else 分支） |
| HIGH-02 | 🟠 | 封包字节字段无上界 → 64KB 上限 + 流剩余检查 |
| HIGH-03 | 🟠 | TLS 1.0/1.1 弃用 → 仅 TLS 1.2/1.3 |
| DEEP-01 | 🔴 | 中和 `最优恢复` 的隐蔽外发逻辑（硬件指纹 POST）|
| DEEP-02 | 🔴 | WebApi 弱签名 `MD5(query+"&")` → `HMAC-SHA256(query, Settings.充值签名密钥)` |
| DEEP-03 | 🟡 | 删除 `------------/` 死代码目录 |

PROTO-01 ~ PROTO-08 协议级安全洞未自动修复（需客户端配合升级），详见审计报告。

### 文档
- 新增 README.md（项目说明、构建指南、端口表）
- 新增 SECURITY_AUDIT.md（完整审计报告）

---

## 版本约定

宽松遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

- **MAJOR** — 网络协议或数据格式不兼容（客户端需同步升级）
- **MINOR** — 新功能 / 大规模重构 / 安全加固
- **PATCH** — Bug 修复 / 小幅调整

当前处于 0.x 阶段，所有 0.X.0 版本可能包含破坏性改动。
